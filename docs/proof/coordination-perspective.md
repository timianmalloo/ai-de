---
id: proof-coordination-perspective
title: "Proof Pack: SH-4.1 — the Coordination perspective (host C)"
type: proof-pack
status: draft
owner: "@timianmalloo"
phase: "addendum-c"
tags: [proof-pack, addendum-c, shell-lane, sh-4, coordination, ruling-84]
links:
  - { to: coordination-addendum-cd, rel: implements }
  - { to: spec-addendum-c-perspectives, rel: implements }
  - { to: note-addendum-c-council-rulings, rel: implements }
  - { to: note-adr-0030-0032-amendment-coordination, rel: implements }
  - { to: ui-review-operator-findings-2026-09-13, rel: relates-to }
  - { to: adr-0030-perspective-registry-and-allow-lists, rel: implements }
  - { to: adr-0031-second-docking-host, rel: implements }
  - { to: adr-0032-perspective-layout-slots, rel: implements }
review-by: 2027-03-13
review-suggested: []
summary: >-
  Ruling 84 landed: Coordination is the fourth Perspective (host C, Ctrl+4, its own slot file and
  default, the rail glyph IconCoordination), the five Loomkeeper kinds are admitted by it alone and
  Coding admits none, the pre-Addendum-C Coding envelope drops them with a report naming
  Coordination and its gesture. P1–P7 each seen red before green; the census walks host C.
---

# Proof Pack: SH-4.1 — the Coordination perspective (host C)

- **Change:** branch `lane/shell-sh4` from `main` `560ea825` (worktree `C:\Projects\ai-de-lane-shell-sh4`).
- **Spec / design:** `docs/notes/addendum-c-council-rulings.md` Ruling 84 (and 83 for what Coding's
  Left becomes — SH-4.2's slice) · `docs/notes/adr-0030-0032-amendment-coordination.md` ·
  `docs/specs/addendum-c-perspectives.md` *Errata after Rulings 83–84* · `DESIGN.md` *Errata after
  Rulings 83–84* · `docs/reviews/ui-operator-findings-2026-09-13.md` §7 SH-4.1 row (the oracles) ·
  `docs/mockups/perspective-shell.html` (the Coordination rail item, host C's states).
- **Tier:** T2. **Fan-out cap:** 3 (reviews read-only).
- **Author / date:** Claude (opus), session `sh-4`, 2026-09-13.

## Goal state (CT19)

- **Goal:** land Ruling 84 — Coordination as the fourth Perspective — end to end, on the branch.
- **Done when:** P1–P7 red → green; the existing suites green (`ShellContrastCensusTests` walking
  the rail's new item and host C's surfaces); the gates green; this pack, the audit entry and the
  push landed; Release rebuilt.
- **Not in scope:** the Coding re-cut beyond what Ruling 84 forces (SH-4.2 — the Bottom question,
  the `session-document` zone rule, `NewSessionPlacement`, the Center's second empty copy, the Left
  extent); CV-5's files; X-3's files beyond the derived-menu contribution; `DESIGN.md`; the mockups.

## E7 — the surface list, written before coding

| # | Surface | File(s) | Writer | Compute reader |
|---|---|---|---|---|
| 1 | Registry row `("coordination", "Coordination", 4, DockHost, "perspective.coordination")`; `All` = 4 rows; `RoutingOrder` gains Coordination (last: it shares no kind, so its position is unobservable until one is shared) | `src/AiDe.Core/Workbench/Perspectives.cs` | this slice | the catalog (`WorkbenchCommands.cs` derives `perspective.coordination`), `KeyGestures.For` (Ctrl+4 from `Order`), the rail (`ItemsSource = PerspectiveSet.All`), `PerspectiveShell` (demands a host per DockHost row), `SurfaceAdmission.AdmittedBy` (the drop report's perspective) |
| 2 | The allow-list column: `sessions · board · leaderboard · ledger · daydreams` → `[Coordination]` | `src/AiDe.App/Workbench/SurfaceContentFactory.cs` | this slice | `DockHost.AdmissionFor` (host C's admission; Coding's refusal), `PerspectiveMenu.For` (the derived View group), `PerspectiveMenu.Resolve` (routing), `watcher.raiseDispute`'s `Admits("leaderboard")` scope |
| 3 | Admission / resolve — derived, no edit | `DockHost.cs`, `PerspectiveMenu.cs`, `SurfaceAdmission.cs` | — | P2's `Resolve("ledger", Coding) == Coordination`; P6's `AdmittedBy == Coordination` |
| 4 | Host C — a third `DockHost.Create`; `Hosts` = A · B · C; the non-A hosts' keyboard-resize route | `src/AiDe.App/Workbench/WorkbenchShell.cs` (X-3 live — claimed for the minutes of the edit; the constructor block, one property, one loop) | this slice | `MainWindow` (composes the presenter over `Shell.Hosts`; themes every host), `PerspectiveShell` (host C's root is the body), `AttachPersistence` (a slot per host), `RenderAll`, `TrackFocusedPane`, `PersistOnEveryChange` |
| 5 | Slot file `<layout>.coordination.zones.json` — derived by `SlotPathFor` (`"." + perspective.Id`) — and the default `CoordinationDefault()`: Left = `sessions` · Center = `ledger` · `leaderboard` · `board` (Ledger first) · Right empty · Bottom empty, collapsed; `daydreams` admitted, not in the default. `CodingDefault()`'s Left becomes empty (`null`) — Ruling 84's own consequence: Coding no longer admits `sessions`, and a default that seeds a kind its host refuses is a `DefaultDropped` at construction (`TheArchitectureAndCodingDefaults_SeedCleanly_WithNoDrops` pins that as a defect). The Bottom and the Center stay as they are (SH-4.2's re-cut, after the Owner's ruling on the Bottom). | `src/AiDe.Core/Workbench/ZoneLayout.cs`, `LayoutPersistence.SlotPathFor` (no edit) | this slice | `ZoneBackedLayoutService.SeedDefault` / `DefaultLayout()`; `LayoutPersistence` (host C's own file); the announcement's pane count |
| 6 | The derived View menu and the palette — by construction; the PS-M1 table gains Coordination: File · Edit · View · Window · Help; its View group *Show terminal sessions · Show message board · Show leaderboard · Show ledger · Show daydreams* + *Raise score dispute…*; Coding's loses those six | `PerspectiveMenu.cs`, `MainMenuBuilder.cs`, `CommandPalette.cs` (no edit expected) | — | P3; `TheRenderedMenuEqualsTheSpecsLiteralTable_ForEveryPerspective` as amended |
| 7 | The rail item + `IconCoordination` (a ring with three nodes) + the gesture in the tooltip | `src/AiDe.App/MainWindow.xaml` (the keyed-resource precedent beside `IconCoding` — PS-R4's recorded deviation); the rail derives the item, the name *"Coordination perspective"*, the tooltip *"Coordination — Ctrl+4"* | this slice | `PerspectiveRailItem.Bind` (`SetResourceReference(Glyph, "IconCoordination")`); P7 |
| 8 | The announcement *"Coordination perspective — 4 panes."* and the title *"<workspace> — Coordination — AI-DE"* — derived from the row and host C's default | `PerspectiveShell.Announcement`, `MainWindow.TitleFor` (no edit) | — | P4's fixture; the attended row |
| 9 | The pre-C restore's report — the drop sentence names the admitting perspective with its gesture: one group *"They live in Coordination (Ctrl+4); open them from its View menu."*; two or more groups, per group *"<captions> live in <Title> (<gesture>)"* | `src/AiDe.App/Workbench/LayoutPersistence.cs` (`DropAnnouncement`) | this slice | P6; ADR-0032 test 1 as extended; the live region |
| 10 | The no-host refusal names every host perspective, derived (*"Switch to Coding, Architecture or Coordination first."*) | `PerspectiveShell.RefuseWithoutHost` | this slice | `PerspectiveShellTests` (the refusal row) |
| 11 | Tests + census: P1–P7; the re-pointed rows (below); the census adds each kind to the host that admits it and walks every host perspective's body | `tests/**` (listed below), `tests/AiDe.App.ContrastProbe/ShellContrastCensus.cs` | this slice | the two test projects; `ShellContrastCensusTests` |

## Claims & evidence

### Claim 1 (P1) — the registry has exactly four rows, Coding · Explore · Architecture · Coordination, each with a bound gesture spelled from its Order
- **Evidence:** `src/AiDe.Core/Workbench/Perspectives.cs` — the `Coordination` row `("coordination", "Coordination", 4, DockHost, "perspective.coordination", …, Landing: ZoneId.Left)`; `All = [Coding, Explore, Architecture, Coordination]`; `RoutingOrder = [Architecture, Coding, Coordination]`. The catalog derives `perspective.coordination` with gesture `Ctrl+4` (`WorkbenchCommands.cs`, unchanged: `..PerspectiveSet.All.Select(...)`); `KeyGestures.For` binds `Ctrl+D4` and `Ctrl+NumPad4` from `Order` (unchanged).
- **Oracle:** `tests/AiDe.Core.Tests/Workbench/PerspectiveSetTests.All_HasExactlyFourRows_CodingExploreArchitectureCoordination_EachWithABoundGesture` — ids, titles, orders, gestures, the fourth row's command id and body, each row's catalog command carrying `Ctrl+{Order}`; a fifth row, a wrong order, or a row whose command carries another gesture fails. Siblings amended: `TheBodiesAreThreeHostsAndOneFullWindowSurface`, `ThePerspectiveGesturesAreCtrlPlusTheRailDigit_AndNothingElseUsesThem` (four gestures, no other digit gesture in the catalog), `TheRoutingOrderIsArchitectureThenCodingThenCoordination`. App side: `PerspectiveMenuTests.Bind_InstallsEightPerspectiveKeyBindings_AtWindowScope` (2 × 4, number row + keypad), `PerspectiveRailTests.TheTooltip_RendersTheBoundGesturesDisplayString` (*"Coordination — Ctrl+4"*).
- **Red observed before green:** yes — `Expected ["coding","explore","architecture","coordination"] · Actual ["coding","explore","architecture"]`; `Expected ["Ctrl+1","Ctrl+2","Ctrl+3","Ctrl+4"] · Actual […"Ctrl+3"]`; `Expected ["architecture","coding","coordination"] · Actual ["architecture","coding"]` (4 failed, 2 passed in the class before the row).
- **Confidence:** Verified.
- **Residual risk:** none for the row; `RoutingOrder`'s position for Coordination is unobservable until a kind is shared with it (recorded on the property).

### Claim 2 (P2) — the five Loomkeeper kinds are admitted by Coordination only; Coding admits none; `Resolve("ledger", Coding) == Coordination`
- **Evidence:** `src/AiDe.App/Workbench/SurfaceContentFactory.cs` — `sessions · board · leaderboard · ledger · daydreams` rows read `Perspectives: [PerspectiveSet.Coordination]`; `DockHost.AdmissionFor` and `PerspectiveMenu.Resolve`/`Admits` unchanged (data-driven).
- **Oracle:** `tests/AiDe.App.Tests/Workbench/KindAllowListsTests.TheFiveLoomkeeperKinds_AreAdmittedByCoordinationOnly_AndCodingAdmitsNone` — per kind: the column literally `["coordination"]`, `Instances.One`; Coding's and Architecture's admission refuse, Coordination's admits and is one-instance; `Resolve` from Coding/Architecture/Explore/Coordination → Coordination; `AdmittedBy` (the drop report's perspective) → Coordination; as a set, exactly these five name Coordination; Coding's remaining column is `codeviewer · diagnostics · prompt · search · session-document · terminal`. `PerspectiveMenuTests.TheAllowListsEqualTheSpecsTable` is §A7 as amended (the fifth column, literally). `Resolve_RoutesAnInadmissibleKindOpen_ArchitectureThenCodingThenCoordination` gains four rows (`daydreams`/`ledger`/`sessions` → coordination; `terminal`/`canvas` from coordination → coding/architecture).
- **Red observed before green:** yes — `Expected ["coordination"] · Actual ["coding"]` (KindAllowListsTests); `TheAllowListsEqualTheSpecsTable`: `Expected ["coding"] · Actual ["coordination"]` once the column moved (re-pointed to the amended table); `Resolve_…(daydreams, architecture)`: `Expected "coding" · Actual "coordination"` (re-pointed).
- **Confidence:** Verified.

### Claim 3 (P3) — Coordination's View menu lists the five Show entries and the dispute verb; Coding's lists neither; a test-time kind admitted by Coordination alone renders there and nowhere else, with no builder edit
- **Evidence:** derived by construction — `PerspectiveMenu.For` joins the catalog and the kind rows; `watcher.raiseDispute` is scoped `Admits("leaderboard")` so it follows the leaderboard (`WorkbenchCommands.cs:232-236`, unchanged); `MainMenuBuilder.cs` unchanged (X-3's file, not touched).
- **Oracle:** `tests/AiDe.App.Tests/MainMenuTests.CoordinationsViewMenu_ListsTheFiveShowEntriesAndTheDisputeVerb_AndCodingsListsNeither` — through the real `MainMenuBuilder.Build`: Coordination's headers `File · Edit · View · Window · Help` (no Prompt); its View lists the five in row order with the verb before the derived group; Coding's, Explore's and Architecture's View list none of the six; Coding keeps *New search · New code viewer · Show diagnostics*; the mutation row `fleet-mutant` renders in Coordination's rendered View only. `PerspectiveMenuTests.TheRenderedMenuEqualsTheSpecsLiteralTable_ForEveryPerspective` is §B3 as amended (Coordination's block added; Coding's View loses the six; every View gains *Coordination perspective*).
- **Red observed before green:** yes — `Expected ["Show terminal sessions", …, "Show daydreams"] · Actual []` (Coordination's View empty while the column read Coding).
- **Confidence:** Verified.

### Claim 4 (P4) — cycling four bodies returns the same instances and invokes no factory twice; host C announces *"Coordination perspective — 4 panes."*
- **Evidence:** `src/AiDe.App/Workbench/WorkbenchShell.cs` — `Coordination = DockHost.Create(PerspectiveSet.Coordination, …)`, `Hosts = [Coding, Architecture, Coordination]`; the non-A resize-key route iterates `Hosts`. `PerspectiveShell` unchanged in mechanism (a host per DockHost row is demanded at construction; `Announcement` counts the host's surfaces).
- **Oracle:** `tests/AiDe.App.Tests/PerspectiveShellTests.CyclingFourBodies_ReturnsTheSameInstances_AndInvokesNoFactoryTwice` — hosts composed by `DockHost.Create` over a counting stub factory (DC-135); A → C → B → Explore → A → C → B → Explore → C shows reference-identical roots, `ActiveHost`/`HostFor` are host C, the Explore factory ran once, the content factory's count is unchanged across the cycle, three distinct services and controllers, the announcement is exactly *"Coordination perspective — 4 panes."*. The three-body form is folded into it (superset). `Execute_RoutesHostCommandsToTheActiveHost_EntryVerbsToHostA_AndOpenersByKind` gains `surface.show.ledger` → host C and `watcher.raiseDispute` → host C's controller.
- **Red observed before green:** yes — first `Sequence contains no matching element` (no fourth row), then with the row alone `Expected "Coordination perspective — 4 panes." · Actual "Coordination perspective — 0 panes."` (the default fell to Coding's table, whose kinds Coordination refused).
- **Confidence:** Verified (headless). P-4's `private_bytes_delta` for host C is the attended row below.

### Claim 5 (P5) — Coordination persists to `<layout>.coordination.zones.json` and its default is Left = Terminal sessions · Center = Ledger · Leaderboard · Message board · Bottom collapsed
- **Evidence:** `LayoutPersistence.SlotPathFor` (unchanged: `"." + perspective.Id`); `src/AiDe.Core/Workbench/ZoneLayout.cs` — `CoordinationDefault()`, `Default(perspective)` dispatching on the id. Schema 1, no field (ADR-0032 rule 1).
- **Oracle:** `tests/AiDe.App.Tests/Workbench/ZoneLayoutSlotsTests.CoordinationPersistsToItsOwnFile_AndItsDefaultIsLeftSessionsCenterLedgerLeaderboardBoard_BottomCollapsed` — the path, distinct from the other two; the host's seed drops nothing; Left `[sessions]`, Center `[ledger, leaderboard, board]` active 0, Right null, Bottom null + collapsed, no daydreams, four surfaces, the captions; every placed kind admitted, one-instance kinds once; a save writes the third file only (schema 1) and a fresh host restores it with nothing dropped. `PerspectiveLayoutSlotTests.HostPerspectives` and `TheArchitectureAndCodingDefaults_SeedCleanly_WithNoDrops` cover host C.
- **Red observed before green:** yes — `Sequence contains no matching element` (no row); then `Assert.Empty() Failure: Collection was not empty` (the seed dropped Coding's kinds).
- **Confidence:** Verified. The arrangement itself is the Owner's, **Inferred** until the operator runs it (Ruling 84).

### Claim 6 (P6) — a pre-C Coding envelope drops the Loomkeeper kinds and reports them naming Coordination and its gesture; no Loomkeeper kind survives in the Coding slot
- **Evidence:** `src/AiDe.App/Workbench/LayoutPersistence.cs` `DropAnnouncement` — the drops grouped by `AdmittedBy`: one group → spec §C4's head, then *"They live in Coordination (Ctrl+4); open them from its View menu."* (singular *"It lives in …; open it …"*); two or more → each pane listed once under its perspective, gesture first: *"<N> live in <Title> (<gesture>): <captions>; …. Open each from that perspective's View menu."* (the UX & Accessibility lens's rewrite of this slice's own mixed-case form — every caption was spoken twice before).
- **Oracle:** `ZoneLayoutSlotsTests.RestoringAPreCCodingEnvelope_DropsTheFiveLoomkeeperKinds_AndReportsNamingCoordination` — the committed golden `Fixtures/pre-perspectives.zones.json`: surviving `["terminal"]`, no Loomkeeper kind, 11 dropped (the seven Architecture kinds + `sessions · board · leaderboard · ledger`), the four `AdmittedBy == Coordination`, the seven `Architecture`, the sentence starts *"11 panes from your saved layout aren't available in Coding — "*, carries *"Sessions (terminal sessions)"*, *"Board (message board)"*, *"live in Coordination (Ctrl+4)"* and *"live in Architecture (Ctrl+3)"*, `PartialRestore`. `TheOperatorsPreCCodingLayout_ReportsTheLoomkeeperTabsInOneSentence_NamingCoordinationAndItsGesture` — the operator's own shape (Center: Ledger, Leaderboard, Sessions, Board; a terminal) yields exactly *"4 panes from your saved layout aren't available in Coding — Ledger, Leaderboard, Sessions (terminal sessions), Board (message board). They live in Coordination (Ctrl+4); open them from its View menu."*; Daydreams alone yields the singular. `PerspectiveLayoutSlotTests.TheWorkspaceOpenRestore_WritesOneRestoreEventPerHost_WithTheDroppedCountAndKinds`: the event carries `dropped_count 11` with `sessions`/`ledger` among the kinds, and host C's `keep-current` row. The session-render replay (`ASessionDocumentIsShownWhereTheOperatorIsTests`) reads `dropped=10` for the operator's 2026-09-11 file.
- **Red observed before green:** yes — `Expected ["terminal"] · Actual ["board","leaderboard","ledger","sessions","terminal"]`; the operator's shape: `Expected "4 panes from your saved layout…" · Actual "Restored your saved workbench arrangement."`.
- **Confidence:** Verified.
- **Deviation recorded:** DESIGN.md's errata render the sentence as *"Coordination opens with them (Ctrl+4); open Daydreams from its View menu."* while the review's P6 oracle names *"They live in Coordination (Ctrl+4)"* — two definitions of one sentence in D3's artifacts. This slice follows the oracle: *"opens with them"* asserts the contents of Coordination's default (Inferred, and false for Daydreams), where *"live in"* is true by the allow-list for every admitted kind. `DESIGN.md` is not this slice's to edit — a finding for the conductor/D3.

### Claim 7 (P7) — `IconCoordination` is a keyed Geometry beside `IconCoding` in `MainWindow.xaml`; every row's glyph parses; the tooltip reads the bound gesture
- **Evidence:** `src/AiDe.App/MainWindow.xaml` — `<Geometry x:Key="IconCoordination">` (a closed ring r=8 at 12,12 and three r=0.8 nodes at 12,4 · 5.1,16 · 18.9,16, which the template's 1.6 stroke fills into solid beads — the mockup's filled nodes without a Fill on the Path; bounds 16 × 16.8 so `Stretch=Uniform` scales it in step with `IconCoding`, the WPF lens's arithmetic); the rail's item is derived (`ItemsSource = PerspectiveSet.All`; `Bind` sets the resource reference `Icon{Title}`, the name *"Coordination perspective"*, the tooltip from `KeyGestures`, and `AcceleratorKey` = the gesture).
- **Oracle:** `tests/AiDe.App.Tests/PerspectiveRailTests.IconCoordination_IsAKeyedGeometryBesideIconCoding_AndEveryRowsGlyphParses` — the markup read as XML: `IconCoding`, `IconArchitecture`, `IconCoordination` in the window's registry, not in App.xaml's; every row's `Icon{Title}` parses to a non-empty geometry; the ring-and-three-nodes has four sub-paths. `TheRail_HasFourDestinationsInOrder_NamedConstantly_WithCodingSelected` (four items, names, 44 × 44, no "Tests"); the Down-wrap, UIA and PageDown rows re-pointed to four; `EveryDestinationsGlyph_ResolvesFromTheWindowsOrTheApplicationsRegistry` derives its titles from the set.
- **Red observed before green:** yes — `Assert.Contains() Failure: Item not found in collection` (no `IconCoordination` key); the three-destination rail rows red on four items before their re-point.
- **Confidence:** Verified (markup + headless rail). The rendered glyph's look is the attended P-1 row.

### Claim 8 — the switch's landing is the row's zone: Coordination → the Left's active tab (Terminal sessions); Architecture → the Center's; and it outlives the pane controls' own Loaded
- **Evidence:** `Perspective.Landing` (`ZoneId?`; Architecture `Center`, Coordination `Left`, Coding null); `PerspectiveShell.LandingSurfaceFor(DockHost)`; `MainWindow.EntryFocusFor` lands there and `ActivateInView`s it; `PerspectiveShell.Activate`'s shown handler defers the entry focus one dispatcher turn (`DispatcherPriority.Loaded`) past the body's Loaded.
- **Oracle:** `PerspectiveShellTests.TheLanding_IsTheRowsZonesActiveTab_LeftForCoordination_CenterForArchitecture_ElseTheViewsActive` — `sessions` for C, `graph` for B, the view's active for A; follows the zone's ACTIVE tab (`domain` after activating it); a collapsed landing zone falls back. `AfterASwitch_FocusIsPlacedByTheEntryFocusHook_OnTheBodysFirstLayoutPass` stays green (it pumps to ContextIdle). Measured in the shown census window (recorded in the census log, never asserted): before — *active after switch = provenance* (Architecture), *= census:coordination:daydreams* (Coordination), the last pane control to realize; after — *= census:architecture:joins* (the Center's active tab), *= sessions*.
- **Red observed before green:** yes — `Expected "graph" · Actual "evidence"` (the view's active was the Left pane, headless).
- **Confidence:** Verified headless and in the shown census window; the operator's run is the attended row.

### Claim 9 — a workspace-open restore into Coding leaves the Center's restored session document active, not the Bottom terminal
- **Evidence:** `src/AiDe.App/Workbench/WorkbenchAdapter.cs` `RestoreActive` — when the pre-render active surface is gone from the model, the Center's active tab is activated, now and again at `DispatcherPriority.Loaded`, guarded by the model.
- **Why it exists (a defect this slice introduced and found):** with Coding's Left empty (Ruling 84 forces it), the session-render replay's `--bind-on-restore` step read *active in view = terminal#b356ea* where the baseline (`560ea825`, measured in a detached worktree) read *= session-document:…*. Traced with a temporary `ActiveContentChanged` stack log: `AvalonDock.Controls.LayoutDocumentPaneControl.OnSelectionChanged → LayoutContent.set_IsActive` — each pane control activates its selection as it realizes, the last wins, and with no Left pane the Bottom's terminal realized last. Confirmed by a Left-occupant experiment (`SH4_EXPERIMENT=left`, a `diagnostics` pane): the document stayed active.
- **Oracle:** `Sessions/ASessionDocumentIsShownWhereTheOperatorIsTests.ARestoredSessionDocumentIsRevivedAndBoundAtWorkspaceOpen` (unchanged assertion: *' active in view=session-document:20260911T175821Z-1edfa710;'*) — red with Coding's Left empty and the old `RestoreActive`, green with the fallback; the first, immediate activation alone was still red (overridden during the 1.5 s settle), the deferred one holds.
- **Confidence:** Verified (the replay probe, three runs).
- **Class:** DC-nnn (SH-4 a) below.

### Claim 10 — the census walks host B's and host C's bodies; 0 below floor
- **Evidence:** `tests/AiDe.App.ContrastProbe/ShellContrastCensus.cs` step 2b — each kind host A refuses is added to the host that admits it; the presenter is switched there; the body settles (`UpdateLayout` · a Background-priority turn · `UpdateLayout`); every surface is activated and walked; the announcement and the settled active surface are recorded; the presenter returns to Coding.
- **Oracle:** `ShellContrastCensusTests.EveryTextPairingInTheComposedShellClearsItsFloor` anchors *tab: Ledger*, *Ledger*, *Message board*, *Terminal sessions*, *Coordination perspective* (the status strip) and *tab: Domain* — a walk that shrinks back to host A fails. 138 → 170 sites, 0 below floor (7/7 census tests green).
- **Red observed:** the first extension (no settle) measured only each host's Left pane (Center and Right tabs +0 sites) and left the rebuilt menus unmeasured (+0, the reach test red: *"the File menu's submenu never rendered a command"*) — the settle fixed both; the anchors were added after and are green against the settled walk (their falsifier is the pre-2b census, which measured none of them).
- **Confidence:** Verified.

## Stage 4 — the reviews (read-only, three, one loop) and what changed after them

| Lens | Verdict | Findings applied in this slice | Recorded, not applied |
|---|---|---|---|
| **Test Architect** (hard veto on P2/P3/P6 being mutation-shaped) | **VETO CLEARED** — all three mutation-shaped, no tautology, the red record credible against the `560ea825` diff | (1) the derived no-host refusal sentence asserted verbatim (`PerspectiveShellTests`); (2) the census's landing lines are asserted per host (`ShellContrastCensusTests`: *active after switch = sessions; zone = Left* for Coordination, *zone = Center* for Architecture, the announcement); (3) `TheLanding_…`'s fallback clauses carry literal failing inputs (`ledger` activated before the Left collapses; `NotNull` on Coding's view active); (4) the adapter's deferred re-activation has a headless test — `ZoneWorkbenchAdapterTests.AfterAWholeArrangementReplacement_…` (and it found the guard wrong: the deferred turn now reads the model's Center active at that moment, so an operator's in-between Center activation is what gets asserted); (7) the Show-daydreams test asserts the sibling's kind literally | (5) host A no longer has a drag row in `WorkbenchDragCompletedHookTests` — Coding's default holds one surface; SH-4.2's re-cut (a session document at Left) restores a Coding pair, handed off; (6) the census anchors' red is observed by data, not by a run: none of *tab: Ledger · Ledger · Message board · tab: Domain* and the *Coordination perspective* text is in the baseline census report (`560ea825`, `sh4-census-baseline.json`; only *Terminal sessions* was, in host A); (8) a duplicate one-instance drop beside an admitted-elsewhere group reads under *"They live in…"* — a design question for D3, untested boundary; (9) the two sentence definitions (Finding 1 above) |
| **UX & Accessibility** (hard veto on the rail item, the announcement, the landing, the drop copy, host C's empty states) | **hard veto CLEARED, soft veto HELD** (three Majors) | (1) the Sessions pane's teaching copy named a *Terminal menu* that exists in no perspective — now *"from the File menu"* (the agent session verbs live there; the mockup was right, the product wrong); (2) the two-or-more-group drop sentence lists each pane **once** under its perspective, gesture first, with number agreement — *"11 panes … aren't available in Coding — 7 live in Architecture (Ctrl+3): …; 4 live in Coordination (Ctrl+4): Sessions (terminal sessions), Board (message board), Leaderboard, Ledger. Open each from that perspective's View menu."* (the single-group form keeps spec §C4's head and the P6 oracle); (3) the landing surface had **no focus target** in its first-run state (an empty or not-available Sessions pane is a `StackPanel` of `TextBlock`s), so the deferred entry focus fell through to an arbitrary tab header — the Sessions pane root and `ListPane`'s empty host are `Focusable` now, named by `Create`/the status; `PerspectiveShellTests.TheLandingSurface_HasAFocusTarget_InHostCsFirstRunState` (red first: *"the Terminal sessions pane has nothing focusable in its not-available state"*); measured after: *focused = StackPanel* on the Coordination switch; (4) subsumed by (2); (5) the glyph's nodes are r=0.8 beads the stroke fills (the mockup's filled nodes), the ring closed, bounds 16 × 16.8 so `Stretch=Uniform` scales in step with `IconCoding`; (7) `AutomationProperties.AcceleratorKey` = the gesture on every rail item | the live region's channel (`NoWrap` + `CharacterEllipsis`, a hover-only tooltip, not focusable — `WorkbenchShell.cs:126-127`, `WorkbenchAnnouncer.cs`, X-3's) truncates the head of every long report; the mockup's own design (wrap, `MaxLines`, focusable) is X-3's seam; NVDA may cut the announcement on the landing's focus change (attended); the three design-record discrepancies (DESIGN.md's sentence, its empty strings, the mockup's Terminal sessions copy) go to D3 |
| **WPF lens** (the glyph; host C; the deferrals) | **NO ESCALATION** (no AllowsTransparency trap; no airspace change — host C holds no HwndHost/WebView2; corners and shadow unchanged) | (1) **its Major, measured and confirmed:** the realization steal was fixed in `RestoreActive`'s gone branch only — with a Center document active, a plain re-render read *rerenderImmediate = the document, rerenderSettled = terminal-1* in the census window (a rename's or an attach's re-render would move the operator's focus to the terminal). One `AssertActive(root, intended)` — activate now, and again at Loaded priority reading the model at that moment — now serves both `RestoreActive` branches and `ActivateInView`; measured after: *rerenderSettled = the document*; `ZoneWorkbenchAdapterTests.APlainReRender_KeepsTheActiveCenterDocumentActive_AfterThePaneControlsRealize`, and both adapter tests observed red against the mutant with the deferred assertion removed (*turn=term-x*; *before=term-x*); (2) the glyph's beads (above); (3) the presenter's comment names the measurement, not a Loaded-order mechanism the lifetime doc contradicts; the census's landing lines are asserted (TA-2); (4) `Settle` waits at ContextIdle; (5) the stale *two hosts / three perspectives* prose | the null-`Landing` (Coding) entry focus still reads the view's active surface — SH-4.2's re-cut of Coding's landing (the session document at Left) is where that rule is decided; a per-keystroke LINQ enumerator on the non-A resize route (a nit; `WorkbenchShell.cs` is X-3's); floating panes in a retained unparented host (a Flagged recollection — a check for the P-4 row: float a pane in C, Ctrl+1, Ctrl+4); startup cost of a third theme parse and `Retokenise` walk, unmeasured |

**Simplifier's delete-list** (run by the author over the diff, the three reviews having spent the fan-out): `delete:` the three-body identity cycle (folded into P4, −45 lines); `shrink:` `RestoreActive`'s two activation branches into one `AssertActive` (the WPF lens's finding, −12 lines net); `yagni:` none — the `Landing` column is one nullable value per row, read by one function; `stdlib:`/`native:` none. `net: −57 lines possible`, taken.

### Claim 11 — the landing surface has a focus target in host C's first-run state, and its copy names a menu that exists
- **Evidence:** `src/AiDe.App/Workbench/SurfaceContentFactory.cs` — the Sessions pane's root `StackPanel` is `Focusable`; `ListPane`'s empty host `Grid` is `Focusable` (already named *"<Title> — <status>"*); the teaching copy reads *"from the File menu"*.
- **Oracle:** `PerspectiveShellTests.TheLandingSurface_HasAFocusTarget_InHostCsFirstRunState` — through the product's factory with no queries and `DockHost.Create` (DC-135): the landing is `sessions`, its content has a focusable, enabled element; every Center tab's not-available host has one too.
- **Red observed before green:** yes — *"the Terminal sessions pane has nothing focusable in its not-available state — the landing falls through"*.
- **Confidence:** Verified (headless); in the census window the Coordination switch now records *focused = StackPanel*.

### Claim 9, extended — the same assertion holds the kept branch and the deliberate open
- **Evidence:** `WorkbenchAdapter.AssertActive` (activate now; re-assert at `DispatcherPriority.Loaded` from the model at that moment; a replaced tree is left to its own render) used by both `RestoreActive` branches and `ActivateInView`.
- **Oracle:** `ZoneWorkbenchAdapterTests.AfterAWholeArrangementReplacement_TheCentersActiveTabIsActive_AndAnActivationInBetweenIsKept` (*render=doc-b operator=doc-c turn=doc-c*) and `APlainReRender_KeepsTheActiveCenterDocumentActive_AfterThePaneControlsRealize` (*before=doc-b immediate=doc-b settled=doc-b*); the replay probe's `ARestoredSessionDocumentIsRevivedAndBoundAtWorkspaceOpen`.
- **Red observed:** the census window before the change — *rerenderImmediate = the session document, rerenderSettled = terminal-1*; the mutant (deferred assertion removed) — *turn=term-x* and *before=term-x immediate=term-x settled=term-x*.
- **Confidence:** Verified.

## Re-pointed tests (the amended rulings, never a weakening)

| Test | Was | Now |
|---|---|---|
| `SurfaceContentTests.The{Board,Leaderboard,Ledger}Surface_…ReachableFromCodingsMenu` | reachable from Coding's menu; not in Coding's default | `…FromCoordinationsMenu`; in Coordination's default; never in Coding's |
| `SurfaceContentTests.TheSessionsSurface_IsInTheDefaultLayout` | Coding's default carries `sessions` | Coordination's does; Coding's carries no Loomkeeper kind |
| `WorkbenchShellTests.Shell_ComposesTheWorkbench…AcrossBothHosts` | *Terminal sessions* in Coding | `…AcrossAllHosts`: not in Coding; host C carries *Terminal sessions · Ledger · Leaderboard · Message board* |
| `WorkbenchShellTests.FindSurfaceId_WalksUpToTheOwningSurface` | `sessions` in host A's manager | host C's manager |
| `WorkbenchShellTests.AShowEntryOpensTheKindOnce_…` | daydreams beside a code viewer in host A | daydreams routed to host C, tabbed into an occupied stack (measured: the Left, beside Terminal sessions — `DocumentPlacementPolicy`'s last rule); the "New" half stays in host A |
| `WorkbenchShellTests.AttachWorkspace_WiresTheWatcher_…` | `shell.Adapter.ContentFor("sessions")` | `shell.Coordination.Adapter…` |
| `WorkbenchDragCompletedHookTests` row | `("coding", "sessions", "terminal-1", Left, Bottom)` | `("coordination", "sessions", "ledger", Left, Center)` — Coding's default holds one surface now |
| `PerspectiveShellTests` fixture; `EveryOpeningCommandPassesThroughTheSeamTests.Presenter()` | hosts A, B | hosts A, B, C (the presenter demands one per DockHost row) |
| `CommandPaletteTests.ThePalette_ListsExactlyTheMenusCommands` | `surface.show.daydreams` listed in Coding | `surface.show.diagnostics` listed; daydreams not (Coordination's) |
| `PerspectiveLayoutSlotTests.APreAddendumCFile_RestoresIntoCoding_…` (ADR-0032 test 1) | 7 dropped, five kinds surviving | moved to `ZoneLayoutSlotsTests` as P6 (11 dropped, `[terminal]`) |
| `PerspectiveLayoutSlotTests.TheWorkspaceOpenRestore_…` | `dropped_count 7` | 11; host C's `keep-current` row |
| `PerspectiveMenuTests.Bind_InstallsSixPerspectiveKeyBindings…` | 6 | `…Eight…`: 2 × `PerspectiveSet.All.Count` = 8 |
| `PerspectiveMenuTests.ThePerspectiveItemsShowTheirBoundGesture…`, `ThePaletteSpeaksAKeystrokeOnlyWhenOneIsBound` | Coding's menu (had *Show daydreams*) | Coordination's; *Ctrl+4* asserted |
| `Sessions/ASessionDocumentIsShownWhereTheOperatorIsTests.ANewSessionRendersInTheOperatorsRestoredArrangement` | `dropped=6` | `dropped=10` (the four Loomkeeper surfaces the operator's file carries) — one assertion and its comment; a CV-adjacent test file, no product file of CV-5's touched |
| `PerspectiveRailTests` three-destination rows | 3 | 4; Down wraps after the fourth; PageDown lands on `items[^1]` |

## Findings (not this slice's to fix)

1. **Two definitions of the drop sentence** — `DESIGN.md` errata (*"Coordination opens with them (Ctrl+4); open Daydreams from its View menu."*) vs the review's P6 (*"They live in Coordination (Ctrl+4)"*). Landed: the oracle's form (Claim 6). D3/the conductor to reconcile the design row.
2. **Daydreams' zone in Coordination is unspecified** — *"Daydreams via menu"* names no zone; `DocumentPlacementPolicy`'s last rule tabs it into the first stack, which is the Left beside Terminal sessions (the master list gains a sibling tab). A placement rule for the reading kinds, if the operator minds, is a design question.
3. **The view's active content after a body is (re)parented is the last-realized pane's selection** — the class behind Claims 8 and 9; registered below. SH-4.2's re-cut (a session document at Left, the Bottom possibly collapsed) changes realization order again and inherits the same controls.
4. **`AShowEntry…`'s Coding half** now opens two code viewers (the "New adds every time" half) — unchanged in meaning.

## Attended rows (RUN-PENDING; the conductor runs what a file or an exit code can evidence)

| Row | Steps | Evidence to record |
|---|---|---|
| **P-1** UIA walk: five interactive rail items, 44 × 44 | 1. `dotnet run --project src/AiDe.App -c Release` (or the Release build below). 2. Accessibility Insights → Live Inspect on the rail: expect *New session* (button) and four tab items named *Coding perspective · Explore perspective · Architecture perspective · Coordination perspective*, each BoundingRectangle ≥ 44 × 44. 3. Hover the fourth: tooltip *"Coordination — Ctrl+4"*. | the UIA dump (names, control types, rects) |
| **P-4** `private_bytes_delta` for host C | 1. Launch; open a workspace; note Private Bytes (Task Manager → Details, or `Get-Process AiDe.App \| % PrivateMemorySize64`). 2. Ctrl+4, wait 5 s, note again. 3. Ctrl+1 · Ctrl+4 · Ctrl+3 · Ctrl+4, note again. Compare with ADR-0031's host-B budget. | three numbers, the delta per switch |
| **Announcement + landing** | 1. NVDA/Narrator running. 2. Ctrl+4: hear *"Coordination perspective — 4 panes."*; the status strip reads *Coordination perspective*; the title *"<workspace> — Coordination — AI-DE"*. 3. Focus is inside *Terminal sessions* (the Left zone's active tab), never on the rail. 4. Ctrl+1 then Ctrl+4 again: the same landing. | the spoken text, `Keyboard.FocusedElement`'s surface, the title |
| **The pre-C restore's report** | 1. With the operator's 2026-09-13 `layout.zones.json` (Ledger · Leaderboard · Sessions · Board in Coding's Center), open the workspace. 2. Read the status strip: *"4 panes from your saved layout aren't available in Coding — Ledger, Leaderboard, Sessions (terminal sessions), Board (message board). They live in Coordination (Ctrl+4); open them from its View menu."* 3. Ctrl+4 shows the four; the Coding slot's `.pre-perspectives.bak` exists after the first save. | the strip's text, the `.bak` |
| **Host C's empty states** | With no watcher store: *Terminal sessions* — *"Session observation is not available — no watcher…"*; *Ledger* — *"Work-episode observation is not available."*; *Leaderboard*/*Message board* — their *not available* copy. (The `state.not-declared` copy D3 designed — *"No episodes scored yet."* etc. — is the product's **with** a store and no episodes; this slice adds no copy: the empty states are the surfaces' own, untouched.) | screenshots of the four panes |

## Defect classes registered (CI1) — placeholders, never the register itself (the conductor allocates; next free DC-182)

- **DC-nnn (SH-4 a)** — *a docking view's active content after a body is (re)parented is whichever pane control realized last, and the arrangement's zone occupancy decides that order*: each `LayoutDocumentPaneControl` activates its selected content from its own `SelectionChanged` as its template applies (traced: `LayoutDocumentPaneControl.OnSelectionChanged → LayoutContent.set_IsActive`), so a model-stated activation placed at the body's Loaded edge — the adapter's `RestoreActive`, the presenter's entry focus — is overridden by the last pane to load, and which pane that is changes when a zone empties (Ruling 84 emptied Coding's Left; the Bottom terminal then beat the Center's restored session document). **Controls (this slice):** `WorkbenchAdapter.RestoreActive` states the Center's active tab again at `DispatcherPriority.Loaded` when the pre-render surface is gone; `PerspectiveShell.Activate` defers the entry focus one dispatcher turn past the body's Loaded; `Perspective.Landing` makes the landing data. **Oracles:** `ASessionDocumentIsShownWhereTheOperatorIsTests.ARestoredSessionDocumentIsRevivedAndBoundAtWorkspaceOpen` (red with the Left empty and the old code — the replay probe), `PerspectiveShellTests.TheLanding_…` (red before the column), the census log's *active after switch* rows (recorded). **Sweep:** every other `IsActive = true` in `WorkbenchAdapter` (`ActivateInView`) is called on an already-realized tree — not this shape; SH-4.2's re-cut re-orders realization again and inherits the controls.
- **DC-nnn (SH-4 b)** — *a census that walks a freshly parented body before its pane controls have realized measures the first pane and reports green over the rest* (DC-135 recurrence 3's sibling — the population shrank and nothing failed): switching the presenter and walking immediately measured only each host's Left pane (Center and Right tabs +0 sites) and left the rebuilt menu bar unmeasured (+0 per menu; the reach test's *"never rendered a command"*). **Control:** `ShellContrastCensus.Settle` (UpdateLayout · a Background-priority turn · UpdateLayout) before each walk, and `ShellContrastCensusTests` anchors named sites in host B's and host C's bodies (*tab: Ledger*, *Ledger*, *Message board*, *Terminal sessions*, *Coordination perspective*, *tab: Domain*) so a walk that shrinks fails; the site count 138 → 170 is on the log.
- **Finding, not a class this slice claims — DC-nnn (SH-4 c, for D3/the conductor)** — *one user-facing sentence with two definitions across a design artifact and its review's oracle* (`DESIGN.md` *"Coordination opens with them (Ctrl+4)…"* vs the review's *"They live in Coordination (Ctrl+4)"*): the implementer must choose, and either choice leaves one artifact stale. A candidate control: the review's oracle strings quoted from `DESIGN.md`'s *Copy added by this section* row, checked by `verify-cited-controls.py`'s string half — the conductor's call.
- **A control that fired, recorded as such:** `PerspectiveLayoutSlotTests.TheArchitectureAndCodingDefaults_SeedCleanly_WithNoDrops` and `DefaultPerspective_PlacesOnlyAdmittedKinds_…` went red the moment the five kinds left Coding while `CodingDefault()` still seeded `sessions` — the ADR-0032 control caught the seed-drop before it reached the product; the fix was the Left = null re-cut (E7 row 5).

## Instrumentation (IO1–IO12)

| Operator question | Emitting source | Observed |
|---|---|---|
| Which perspective, from which, by what trigger, did it switch? | `WorkbenchDiagnostics.ShellMode` — `shell.mode` (unchanged mechanism; the fourth row is a new value) | in the census window: `{"evt":"shell.mode","mode":"coordination","from":"architecture","trigger":"perspective.coordination","first_entry":false,"outcome":"switched"}` |
| How long did the switch take to show? | `shell.mode.shown` `duration_ms` | `11.6` ms for the Coordination switch (a shown window; not a budget assertion — P-8 stays attended) |
| What did the restore drop, and where do the drops live? | `WorkbenchDiagnostics.LayoutRestoreReport` — `layout.restore` with `dropped_count`, `dropped_kinds`, `error_code`; the live region's sentence | `dropped_count 11` for the golden pre-C file; `dropped=10` for the operator's replay; host C's `keep-current` row |
| Which host seeded a drop at construction? | `layout.restore` `placement: default-filtered` (unchanged) | none — every host seeds cleanly (asserted) |
| Where did focus land after the switch? | **gap** — no event names the landing surface; the census records `active after switch` in its log (a test artefact, not the product's telemetry) | *sessions* for Coordination, the Center's active tab for Architecture, in the census window |
| `private_bytes_delta` for host C | **attended** (P-4) — no in-product emission; ADR-0031 measured host B by hand | not recorded |

The landing gap: a `shell.mode.landed` line (surface id, zone, whether the entry focus placed it) would close it on the normal path — one call in `EntryFocusFor`; not added here because `WorkbenchDiagnostics.cs` is X-3's live file. Named for the conductor.

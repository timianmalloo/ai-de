---
id: proof-atlas-views-grounding
title: "Atlas five-view grounding and coordination evidence"
type: proof-pack
status: draft
owner: "@timianmalloo"
tags: [atlas, coordination, proof]
links:
  - { to: plan-atlas-views, rel: documents }
  - { to: coordination-atlas-views, rel: documents }
review-by: 2026-12-15
summary: "Observed scope, isolation, peer handoff and graph review; no product acceptance."
---

# Boundary

This is a **grounding Proof Pack**, not proof that Sequence, Activity, domain, layer or Azure
views work. User assigned these five views on2026-09-15. ER, E3/E4, GHCP Class qualification,
Grok Solution tree and recovery/main publication remain separate.

## Observed records

| Item | Evidence | Observation |
|---|---|---|
| Isolated programme | conductor/atlas-views from c46e112a | coord worktree new registered codex-atlas-views-conductor; primary dirty files preserved |
| Separate decision authority | owner/atlas-views, Owner8c9fa47c | T2 and five-view boundary; separate design lanes admitted, implementation not admitted |
| E1 lane | atlas/behavior-views, codex-atlas-e1-behavior | Distinct provisioned tree; Sol high; Sequence/Activity source-contract mapping |
| E2 lane | atlas/architecture-views, codex-atlas-e2-architecture | Distinct provisioned tree; Astra high; design e2c17251 inspected |
| Layer installed | coord doctor; pack doctor |11 patterns/effective drivers;10 PASS/3 WARN/0 FAIL; no clone reinstall |
| GHCP transfer | req-01M2KA6YK6SJBRF4MF27H0J8XE | User assignment and exact preserved recovery boundary; Codex acceptance recorded |
| Core acknowledgement | req-01M2KA74EQYXWCZB5GMRNXNWN1 | Per-lane exact file/signature grants, section2 surface rows, integrator seams required |
| Grok acknowledgement | req-01M2KAP11RXRJ3VN14K3E9Q1QK | No query overlap beyond existing GraphAsync/DescribeAsync; no solution-tree kind on Codex trees |
| Pinned intent |92e025ae8cacd0a2fc8580f865a2172c4a9bdeb4 | Opened US-E6/7/8 and architecture/static design; candidate intent, not product acceptance |
| Graph review | review/atlas-views-design e07c9e59 | Independent grounding PASS; explicit evidence-node omission blocks implementation |
| Repair and review | Conductor137d4fe5; reviewer59bac4ee | E1-P/E2-P added between implementation and review; focused reviewer cleared that finding only |

The Conductor opened source independently: SequenceModel lacks the richer evidence contract;
InteractionResult carries type-level source revision/bounds; BicepExtractor emits declarations
and explicit dependsOn, not a complete resource topology; BoundedContextMap explicitly groups
symbols but supplies no DDD role/invariant schema. These are **Verified source observations**,
not runtime measurements. Neither namespace nor table identity establishes a domain aggregate.

## Actual checks and limits

- `docs-graph.py derive`:498 artifacts after initial plan; `validate`:0 defects,0 orphan,
  0 index drift,66 existing review suggestions. Suggestions were read, not called failures.
- `tools/regenerate-derived.py`: official generator order ran after audit; conflict-marker,
  derived-view, site-figure,225-class defect register, audit-readability and registry coverage
  checks passed. At initial checkpoint the audit renderer read714 audit+147 change entries.
- The commit floor checked9 staged paths for45aac3e3 and5 paths for137d4fe5: free or ours.
- Product tests, real native views, wire conformance, performance and mutation evidence have
  **not** run. No implementation or native acceptance is asserted by these documentation checks.
- `docs-graph.py context --id kb-graph-and-loop-engineering` returned ROOT_NOT_FOUND. The
  referenced evidence directory is absent; fallback uses installed GO instructions and actual
  local audit history. Missing pack knowledge is a finding, not new programme scope.

## Corrections and executable prevention

Independent review identified evidence hidden inside implementation/join prose. **Class:**
verification-obligation loss during graph transcription (existing DC-118 shape). **Sweep:**
checked both E1 and E2 graph paths. **Derive:** proof production must be a separately named
exit before independent review. **Prevent:** E1-P/E2-P reject missing, unexecuted or contradictory
receipts; independent review59bac4ee checked both node table and DAG. Product controls remain
future work and this planning correction is not a substitute for them.

## Remaining acceptance and seams

### Contract-spike checkpoint

Owner57a09ed6 chose explicit validated semantic declarations and source-evidenced alias identity.
E1 design755d0347 and E2 designe2c17251 are frozen design inputs, not product acceptance.
Ruling121 grants exact synthetic spike files under requests req-01M2KBCP7JY9PJTC63PD04WPT2 and
req-01M2KBCP9A8893CWVDDGKKTBYS. Both lanes are executing bounded six-group spikes; public
production signatures, src/tests, solution membership and dependency versions remain excluded.

Common baseline `python tools/verify-project-coverage.py` at Conductor137d4fe5 printed:

```text
39 tracked project(s) — 18 in AiDe.sln, 21 outside, 0 exempt.
built 21 project(s) outside the solution in 26s
verify-project-coverage: OK — 39 tracked project(s), 18 in AiDe.sln; every one compiles.
```

Linux prerequisites were measured: Ubuntu24.04 x86_64 existed; dotnet was absent. The official
Microsoft non-admin installer installed exact SDK10.0.303 in
`/home/timmall/.cache/codex-atlas-dotnet-10.0.303/sdk`; `dotnet --info` observed SDK10.0.303,
MSBuild18.6.14 and runtime10.0.11 on linux-x64. No system/profile change. Initial `/tmp` installer
path was absent on the subsequent invocation; cause unestablished, switched to the explicit
task-specific user cache and verified the executable. Installation is not spike compatibility proof.

Opened `.github/workflows/build.yml` shows project-coverage only in windows-latest, not the
Ubuntu job. Core independently acknowledged this in req-01M2KBN3P0A1V2MV3ADXQBZ5J2: manual
Linux build/run satisfies Ruling121's evidence requirement, but is not proof CI exercises the
spikes on Linux. Workflow changes are out of scope. E1 Linux verification remains pending.

### E2 Linux compatibility observed

Frozen source commit `55df5b6e75e2a7b52f2f5aeb5433e19383196570` was exported with
`git archive` (global.json, Directory.Build.props/rsp, Directory.Packages.props and the exact
spike directory) into a task-specific Linux user cache. Windows bin/obj were not copied.
The SDK executable was `/home/timmall/.cache/codex-atlas-dotnet-10.0.303/sdk/dotnet`.
From the exported root, the observed command was:

```text
dotnet build spikes/atlas-architecture-contract/AtlasArchitectureContractSpike.csproj -c Release --artifacts-path /home/timmall/.cache/codex-atlas-verification/e2-55df5b6e-output --nologo
Build succeeded.
    0 Warning(s)
    0 Error(s)
Time Elapsed 00:00:01.84
```

Running the output DLL with that same SDK returned exit 0. The Conductor read all 23 emitted
checks, including specific negative diagnostics before the positive cases. The final output was:

```text
UNRESOLVED fully-known-deployment-positive: admitted Bicep subset supplies no actual subscription/resource-group identity; no invented tuple injected.
PASS 23 contract checks; six fixture groups observed; full deployment equality remains UNRESOLVED (not US-E8 acceptance).
```

This is one observed Linux compatibility run, not CI coverage or native acceptance. Full
output is the same 23-case sequence recorded in the E2 RESULT.md at the frozen commit.
Independent review and after-change project-coverage timing remain pending.

Owner read-only disposition: preserve missing deployment context as an explicit unresolved
result; request an admitted source-bound producer, not parameter defaults or invented scope.
Source literal scope-kind cannot establish actual subscription/resource-group identity.

Installer contract reference: <https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet-install-script>.

1. GHCP accepted foundation SHA or explicit candidate-base agreement. Requests
   req-01M2KA74D0TGMKV5G0XW1VBHDY and req-01M2KB7P9DK7MQB8661YD83S7M.
2. Owner decisions on explicit domain/layer intake and source-evidenced resource alias identity;
   exact shared identity, receipt, bounds and renderer contracts.
3. Exact per-lane files/signatures and section2 surface grants; jointly satisfiable guard table.
4. Frozen design and native mock/fixture/performance contracts plus independent T2 gates.
5. Red-first implementation, real serialized native proof, independent review, official joins
   and integration handoff. No Main publication grant is inferred.

## Prior programme publication

Previous recursive ownership and audit-verifier implementations/proof documents are published.
At remote mainc46e112a,12 original review-history rows were also published. A JSON comparison
of44 Codex rows found0 semantic differences and exactly1 missing prompt record. Core committed
that final record unchanged on conductor/watch-0915 for its next join; it is not yet observed on
remote main. The finite13-row publication handoff remains separate from this new programme.

## Retention

Programme and worker trees are retained for active design, original audit provenance and later
dependency-ordered joins. No peer worktree or dirty primary file was cleaned up. No AIDE_*
environment was present, so registered episode emission is unavailable in this harness session.


## Corrected spike compatibility checkpoint

| Frozen lane | Linux Release build | Observed run | Windows project coverage |
|---|---|---|---|
| E1 5d361f2a9de2ebdcc7a255d33ad31d96a3a2e0f1 | 0 warnings, 0 errors; 3.49s | Six fixture groups; three negative-first controls; exact Roslyn4.14/runtime10.0.11 signatures printed | 40 tracked /18 solution /22 outside /0 exempt;22 outside built in26s |
| E2 901894112b50d5cdd8cd272f1cb4cb3e886366a7 | 0 warnings, 0 errors; 1.91s | 28 checks, named negative cases first; full deployment equality explicitly unresolved | 40 tracked /18 solution /22 outside /0 exempt;22 outside built in26s |

Both Linux sources were exported at the exact commits with git archive, including repository SDK/compiler/package props and only the relevant spike directory. Separate Linux output directories avoid Windows build artifacts. Commands used the same exact SDK executable and build/run shape as the earlier E2 receipt. E1 output ends SUMMARY fixture_groups=6 failures=0 with the static-reconstruction disclosure. E2 output ends PASS28 contract checks, six fixture groups, full deployment equality remains UNRESOLVED. The Conductor read complete output, not only exit codes. Independent spike review remains pending. These results do not prove CFG, native view behavior, authorization or full US-E8.

The before/after gate comparison is per lane against baseline39/18/21 in26s. A combined two-spike candidate has not been joined or measured; its landing gate remains required. No measured speedup or CI Linux coverage is claimed.


## Independent spike clearance and design continuation

Reviewer acdf4894c9a957600bfd6e63b6ab59319dad92ff clears E1 5d361f2a only as source/syntax/symbol feasibility, and E2 90189411 only as the repaired experimental projection. The Conductor opened the committed receipt. It records the normal outputs and three E2 subject faults failing the exact dependent assertions. The reviewer excluded an initial parallel-build file-lock failure and credited the subsequent already-built run. No CFG, deployment equality, native or product acceptance was granted.

Owner a766afa88cb4cbbd20c84dc383b213a4ab4e5e94 preserves governing G6 and admits two design-document revisions under one logical envelope. Eight design P0s remain: archetype mapping; E2 typed schema; shared request/observation/completion/Restore contract; foundation/grants/joint guards; executable hard-state mock harnesses; numeric targets/caps/protocol; E1 control-flow paging; per-view E2 native accessibility and consistency oracles. Proposed16ms UI-work target is normative and unmeasured. Both revisions use independent existing worktrees; exact new mock paths await req-01M2KCTTG4ZWAC2K01PB1HYWGK. Upstream grammar discrepancy is reported by req-01M2KD0HNAYKZPBRW2SM9PR63N.

The generated audit-data lease mistakenly held by E1 blocked Conductor and E2 commits across worktrees. The holder explicitly released it; commits then passed identity-bound hooks. No TTL expiry was treated as permission. Shared-register recurrence request req-01M2KCDFNJWXTPRB3G55PJ7Z2E remains pending; no completion claim is made for that obligation.


## Design-text clearance and continuing proof

Revised E1 83e1139b and E2 15b53fe9 cleared at design-text level by d062650d. Owner admits finite structural/page and typed-relation experiments within Ruling121. E1 independent oracle ledger precedes authoring; E2 source experiment runs independently. No mock file, shared register or product authoring has begun beyond its grant. Human exact3doc-path question remains pending. Main-watch route is copilot-main-watch; accepted-Atlas publisher remains recovery closer. Main integration authority does not transfer by Claude usage cap.


## Ownership-grounding correction and recurrence capture

The sole register section 2 explicitly makes docs/lessons/defect-classes.md shared append-only. The earlier request for an additional Core grant was unnecessary and is resolved as a correction, not as implied consent.35 new lines append dated recurrences under existing DC-127, PACK-Q and agreement-grounding entries; every prior line/status/count remains. Register gate passed 225 classes. Existing executable controls are named with observed reds; no new automatic retry/permission detector is claimed. Only the two Design-owned mock HTML files still await the requested grant.

Site wrappers are authored, with generated data-figure contents. After the watcher observed the earlier conflicting lease absent (cause Unknown), exact own claims and an inspected figure-only diff allowed commit902ec1c4; all own claims were released. No other session lease or ownership was changed. git check-attr reports no automatic merge driver for the lesson register; the integration handoff must use the documented coord merge-register mechanism, not a hand merge. No coordination framework change is made here.


## E2 relation experiment final qualification

Final E2 source is ed7efe792beb1e3a1a64942e58c1f669aea7cb68. Independent reviewer dcc0e35893cc717996ade44d673bd03457fbea10 cleared the bounded synthetic relation carrier after a full-binding assertion repair. The Conductor opened that receipt and the final source diff. The repair fixes expected anchor ID, target, scope, hash, start and length independently; the new fault preserves ID/target but corrupts the remaining binding fields.

Conductor exported that exact commit and the same repository compiler/package configuration via git archive. On Ubuntu 24.04 with pinned SDK 10.0.303, Release build passed with zero warnings/errors in 1.90s. Running the emitted DLL completed 57 normal checks, including the 224-row boundary across seven collections, over-limit refusals, typed endpoints, current declared relationship remaining declared, and both projected relation rows with complete anchors. The explicit --fault relation-corrupt-binding run exited 1 at relation-produced-target-and-current and printed wrong-scope/wrong-hash/start1/length2. Its failure is expected evidence, not a failed normal build.

Final Windows project-coverage at this source: 40 tracked projects, 18 in solution, 22 outside, 0 exempt; all 22 outside compiled in 21s. Earlier 63e68e8f had 25s and the pre-spike baseline 26s. These are individual printed gate observations, not a speedup estimate. Final combined two-spike candidate is still unjoined/unmeasured. No production authority, full deployment identity, wire/native behavior, measured view performance or product acceptance is claimed. Source fixture assertions are synthetic data, not real admitted provenance.

## Final structural and semantic experiment checkpoint

Independent final E1 receipt ce4f913ac5179459ba5805263ad443bad5e911d8 clears the bounded relation-set/page experiment. The Conductor read the committed findings, including the narrowed display-order exclusion and the reviewer's independent output observations. E2 focused clearance is53ed3844. Neither clears product admission.

This checkpoint supersedes the earlier E2 semantic-row clearance where incomplete. Independent review found that the dependency row's Basis field was not asserted. Source a9d86fc1350ac4c74d5d8ff39e9f39685b890f14 fixes two independent expected rows and compares every semantic field, evidence record and reference sequence. Review 53ed3844 independently observed 57 normal checks and the new basis-only fault failing the dependent oracle. The author recorded its false pass before repair; the Conductor did not independently replay that historical red.

E1 source 1ab5d9e985fd86ef75adb8d2a11c72fe05b530cc derives its graph from Roslyn syntax and symbols; the Conductor opened StructuralBuilder, ProjectPage and the independently fixed expected tables. The subject does not read expected graph tables. Owner 18cc6cb6f9933bf1772e5c622956df30bb842052 narrows relation qualification to exact set preservation: lexicographic identity sorting is comparison canonicalization, not the design's endpoint-ordinal/kind-rank/arm display ordering. Author receipt 96c115e09641520f47678b8c0408ffede452a9d9 explicitly retains that missing display proof. A git diff confirmed Program.cs and csproj unchanged from the executed source. Primary ordinal and auxiliary checks remain unchanged; full P0.7/native acceptance is not granted.

| Exact source | Linux Release build | Linux output inspected | Final Windows project coverage |
|---|---|---|---|
| E1 1ab5d9e9 | 0 warnings/errors; 2.48s | Six legacy groups/three negatives; four structural groups; 13 primary +17 auxiliary =30 nodes;30 relations;28 pages;16 recompositions;10 oracle rejections across8 subject faults; D5 and five invalid-request refusals | 40 tracked/18 solution/22 outside/0 exempt;22 built in21s; exit0 |
| E2 a9d86fc1 | 0 warnings/errors; 1.91s | 57 normal checks exit0; both complete semantic rows printed; basis-only mutation exit1 at relation-produced-target-and-current, with the altered dependency Basis visible | 40 tracked/18 solution/22 outside/0 exempt;22 built in25s; exit0 |

Linux used exact git archives containing global.json, Directory.Build.props/rsp, Directory.Packages.props and the one spike directory. SDK10.0.303/runtime10.0.11 ran on Ubuntu24.04, with isolated Linux artifacts. Commands used the prior documented build/run shape, export directories e1-1ab5d9e9 and e2-a9d86fc1. E1 normal output reported elapsed_ms=64.822 for its entire synthetic structural harness; this is not native latency or p95. Full E1 raw fixture/page output is preserved in its RESULT.md; Linux reproduced its fixed counts, spans, predicates, stubs and recompositions. Full normal and fault output was read, not reduced to an exit-code claim.

The earlier baseline remains39/18/21 in26s. Each lane adds one outside-solution project; combined-candidate coverage is still a required future join check. No platform timing implies a speedup. Existing seven E2 faults were not rerun by the Conductor because the reviewed diff only adds full-row equality and the eighth fault; prior evidence remains pinned. Product foundation, exact grants, real wire/receipt contracts, production display ordering, full deployment identity, executable mockups, native accessibility and measured performance remain open. Only the two Design-owned mock HTML paths await the pending human grant; lesson recurrence writes are already authorized shared append-only work.

---
id: proof-coding-recut-left-dock
title: "Proof Pack: SH-4.2 — Coding's re-cut and the left dock"
type: proof-pack
status: accepted
owner: "@timianmalloo"
phase: "addendum-c"
tags: [proof-pack, addendum-c, shell-lane, sh-4, sh-4-2, coding, left-dock, console-document, reconcile, ruling-83, ruling-88, ruling-89]
links:
  - { to: coordination-addendum-cd, rel: implements }
  - { to: spec-addendum-c-perspectives, rel: implements }
  - { to: note-addendum-c-council-rulings, rel: implements }
  - { to: ui-review-operator-findings-2026-09-13, rel: implements }
  - { to: proof-coordination-perspective, rel: refines }
  - { to: proof-editor-rest, rel: relates-to }
  - { to: proof-console-coalesce, rel: relates-to }
  - { to: mockup-perspective-shell, rel: relates-to }
  - { to: defect-classes, rel: relates-to }
review-by: 2027-03-13
review-suggested: []
summary: >-
  Rulings 83, 88 and 89 landed on the Shell lane: a new session docks in Coding's Left zone (not
  maximized; NewSessionPlacement retired; the kind row carries the zone rule), CodingDefault is Left
  empty at 1.3 of the Center · Center empty · Bottom one terminal collapsed, the Center's empty copy
  is two true sentences by state, the reconcile reads the model's zones and anchors panes by identity
  (F-1: a drag while a collapsed zone holds panes or a stack is maximized is applied), the Console
  split is a `console` document in the Center that closes with its session (C5), Coding lands on the
  session at Left, and the Left extent and the thread's density are measured on the composed tree.
  L1–L6, F-1, C5 and the landing each seen red before green.
---

# Proof Pack: SH-4.2 — Coding's re-cut and the left dock

- **Change:** branch `lane/shell-sh4-2` from `main` `36b7210e` (worktree `C:\Projects\ai-de-lane-shell-sh4-2`).
  Commits `9b5d1062` (Core: the re-cut, F-1's reconcile, the adapter's pane identity), `6cb916e4`
  (App: the left dock, the Center's copies, the console document, the landing, the retirements,
  the DESIGN.md seam rows), then the review and close commits listed under *Gates at close*.
- **Spec / design:** `docs/notes/addendum-c-council-rulings.md` Rulings 83, 88, 89 (84 for what
  SH-4.1 did); `DESIGN.md` §"Errata after Rulings 83–84" (the Coding re-cut row, the Center's copies,
  the landing row, the drop sentence) and the thread contract's "Turns at least half visible" row;
  `docs/mockups/perspective-shell.html` (the Coding re-cut); the review's §7 SH-4.2 row
  (`docs/reviews/ui-operator-findings-2026-09-13.md`) — the oracles below verbatim; CV-5.2's seam
  request `req-01M2E1HEMCMHRW1AX4SY8RJFQ7` (`.agents/requests.jsonl`) for the `console` row.
- **Tier:** T2. **Fan-out cap:** 3 (reviews read-only).
- **Author / date:** Claude (opus), session `sh-4-2`, track SH-4.2 under `conductor-addendum-c`, 2026-09-13.

## Goal state (CT19)

- **Goal:** Coding's re-cut and the left dock per Rulings 83/88/89 — a new session docks Left (not
  maximized), `NewSessionPlacement` retired, `CodingDefault` = Left empty · Center empty · Bottom one
  terminal collapsed, the reconcile's blindness fixed (F-1), the `console` kind row + Center
  admission (C5), the Center's two copies, Coding's landing, the 96ch Left extent measured.
- **Done when:** L1–L6, F-1, C5, the landing red → green; the gates green; this pack + the audit
  entry; pushed to `lane/shell-sh4-2`; Release rebuilt.
- **Not in scope:** the Conversation lane's files beyond the seam request's named touches
  (`SessionDocumentSurface.Split` is the console document's instance; the header's Console toggle
  dispatches the verb); a merge to `main`; the mockups; the defect register (placeholders only).

## E7 — the surface list, written before coding

| # | Surface | File(s) | Writer | Compute reader |
|---|---|---|---|---|
| 1 | `CodingDefault` — Left empty at `CodingLeftExtent` (1.3 ⁄ 2.3), Center empty, Bottom `[terminal-1]` collapsed, Right empty | `src/AiDe.Core/Workbench/ZoneLayout.cs` | this slice | `ZoneBackedLayoutService.SeedDefault` (every fresh Coding host); `ZonesToTree` (the projection omits the collapsed Bottom); the persistence seed |
| 2 | The zone rule — `SurfaceKind.Zone` (`session-document` → Left; `console` → Center) | `src/AiDe.App/Workbench/SurfaceContentFactory.cs` | this slice | `WorkbenchShell.OpenReferenceDocument` (`ZonesToTree.StackIdOf(zone)` → `AddSurface`); a restore never reads it |
| 3 | The create path — `OpenSessionDocument` → the row's zone, then `ActivateInView`; `MainWindow.NewSessionAsync` announces the open + the bind, no maximize; `NewSessionPlacement` deleted; the ComposerProbe replays end where main does (`--maximize` gone) | `WorkbenchShell.cs`, `MainWindow.xaml.cs`, `tests/AiDe.App.ComposerProbe/*` | this slice | L2/L3; `ASessionDocumentIsShownWhereTheOperatorIsTests` (the replay) |
| 4 | The landing — `Perspective.Landing` Coding = Left; `LandingSurfaceFor` falls back to the Center's active tab (the placeholder when empty) | `src/AiDe.Core/Workbench/Perspectives.cs`, `src/AiDe.App/Workbench/PerspectiveShell.cs` | this slice | `MainWindow.EntryFocusFor` (Ctrl+1); the census's *active after switch* row |
| 5 | The Center's copies — `CenterEmptyState.CopyFor/Build`, composed by the shell for the projection's placeholder; the adapter rebuilds the placeholder every render | `src/AiDe.App/Workbench/CenterEmptyState.cs`, `WorkbenchShell.ContentFor`, `WorkbenchAdapter.Render` | this slice | the rendered Center pane; the landing's focus target; the census walk |
| 6 | The reconcile — `TryMapByPosition` identity-first (`ILayoutPaneSerializable.Id` written by `BuildPane`, read by `MapNode`), share-of-owned majority, the placeholder = Center and never in the model, collapsed-holding zones keep content, a drop beside a rail joins + expands; `ReadLayoutFromView` knows the placeholder; the refusal sentence names no removed cause | `src/AiDe.Core/Workbench/ZoneBackedLayoutService.cs`, `src/AiDe.App/Workbench/WorkbenchAdapter.cs`, `WorkbenchShell.RefusedReconcileAnnouncement` | this slice | every native drag (`ViewArrangementChanged` → `ReconcileViewIntoModel`); `LayoutReconcile` diagnostics |
| 7 | The `console` kind — the row (Coding, Many, verb `session.console`, Zone Center), the catalog verb (View, `Ctrl+K, Ctrl+L`, scoped `Admits("session-document")`), the controller case → `SessionConsoleRequested`, the shell's `OpenSessionConsole`/`ShowOpenConsole`/`FocusConsole`, `ConsoleFor` (the factory's resolver), `ConsoleDocumentHost` (the document's `Split`, hosted; follows the read model), the seam `SessionDocumentSurface.ConsoleRequested` (the toggle and *Open the log* dispatch it), close-with-session via the adapter's `SurfaceClosed` | `SurfaceContentFactory.cs`, `WorkbenchCommands.cs`, `WorkbenchController.cs`, `WorkbenchShell.cs`, `Sessions/ConsoleDocumentHost.cs`, `Sessions/SessionDocumentSurface.cs` | this slice (content CV-5.2's) | C5; `DockHost.AdmissionFor` (Coding admits `console`; B and C refuse); `PerspectiveMenu` (the View entry) |
| 8 | The census + tests — the allow-list tables (`KindAllowListsTests`, `PerspectiveMenuTests` §A7/§B3), the seam scan's new head, the drag-hook's Coding row, the re-pointed terminal tests, the window-close test's seeded slot | `tests/**` | this slice | the gate |

## Claims & evidence

### Claim 1 (L1) — `CodingDefault` is Left empty at 1.3 of the Center · Center empty · Bottom one terminal, collapsed
- **Evidence:** `src/AiDe.Core/Workbench/ZoneLayout.cs` — `CodingLeftExtent = 1.3 / 2.3`; `CodingDefault()` Left `(null, CodingLeftExtent, Collapsed: false)`, Bottom `([terminal-1], 0.30, Collapsed: true)`; shape `Left:-|Right:-|Bottom:[terminal-1@0]/collapsed|Center:-|float:`.
- **Oracle:** `tests/AiDe.Core.Tests/Workbench/ZoneLayoutTests.CodingDefault_IsLeftEmpty_CenterEmpty_BottomOneTerminalCollapsed` (the extent as the named constant and as the ratio 1.3 ± 1e-6; the Bottom collapsed; the shape literal); `ASessionOpeningIntoTheEmptyLeft_KeepsThe1Point3Cut_InTheProjectedColumns` (the projected columns' weights ratio 1.3 after `OpenPane(Left)`; no Bottom row).
- **Red observed before green:** yes — *Expected 0.5652…, Actual 0.22* and *Expected Horizontal, Actual Vertical* (the Bottom row was rendered).
- **Confidence:** Verified. **Residual risk:** the extent's *fit* is Claim 6's; this pins the model.

### Claim 2 (L2) — a new session document opens in the Left zone, docked, `Maximized == null`, active; a reopen follows the same rule
- **Evidence:** the `session-document` row carries `Zone: ZoneId.Left`; `OpenReferenceDocument` reads the row's zone before the placement policy (`mode = "zone-rule"` in the `layout.mutation` record); `OpenSessionDocument` then `ActivateInView`s the document; `MainWindow.NewSessionAsync` announces `opened + " " + BindComposer(...)` and nothing else.
- **Oracle:** `tests/AiDe.App.Tests/Sessions/SessionDocumentPlacementTests.ANewSessionDocument_OpensInTheLeftZone_Docked_WithMaximizedNull` — through the real shell in a MainWindow-shaped frame (DC-135): zone Left, `Maximized` null, no sibling zone's collapsed state changed by the open, the Left kept `CodingLeftExtent`, the content rendered, the view's active surface is the document, the sentence starts *Session “Front door” opened.* and never contains *aximized*. `AReopenedSessionDocument_FollowsTheSameZoneRule_AndRearrangesNothing` — a sibling tab at Left, the Bottom still collapsed, the shape changed (DC-016). `TheSessionDocumentKindsRow_NamesTheLeftZone` — the data.
- **Red observed before green:** yes — *Expected Left, Actual Center* (the placement policy's "any stack" fallback tabbed it into the Center); the row's `Zone` was null.
- **Confidence:** Verified.

### Claim 3 (L3) — `NewSessionPlacement` has no callers, then no definition; the create path carries no maximize
- **Evidence:** `src/AiDe.App/Workbench/Sessions/NewSessionPlacement.cs` deleted; `MainWindow.GiveTheNewSessionTheWholeTree` deleted; `tests/AiDe.App.Tests/Sessions/ANewSessionTakesTheWholeTreeTests.cs` (Ruling 47's condition) deleted and replaced by Claim 2's file; the ComposerProbe's three call sites and its `--maximize` option removed; `ComposerHostIntegrationTests` run the probe without the flag (main's choreography and the F5 tree are one now).
- **Oracle:** `SessionDocumentPlacementTests.NewSessionPlacement_IsGone_AndTheCreatePathCarriesNoMaximize` — scans every `.cs` under `src` and `tests` for the token (itself excluded) and `NewSessionAsync`'s body for *Maximized* / *WholeTree*.
- **Red observed before green:** yes — the type and the window's call were present; then once more on a comment in `Program.cs` that still carried the token (reworded).
- **Confidence:** Verified.

### Claim 4 (L4) — the Center's empty copy is two true sentences by state, never *No session open* while one is open at Left
- **Evidence:** `src/AiDe.App/Workbench/CenterEmptyState.cs` — `CopyFor(host, zones, isLive)`: no session surface in any zone → *No session open.* + the **New session** action (named *New session*, the catalog's `Ctrl+N` as its accelerator and as a visible chip) + *Or open a recent one from File → Recent sessions.*; ≥ 1 live session → *The session is docked at the left.* (plural *The sessions are docked at the left.*; another zone named — *the bottom*; several — *the left and the bottom*; a collapsed rail holding it — *…docked at the left, collapsed — expand the rail to reach it.*) + *Code viewers, prompt drafts and search open here, from the View menu.* with **no first action** — the heading is a `ThreadText` (a Control-view text element), focusable, its sentence the accessible name and the second line its help; session surfaces with no live document → *Nothing open here.* + *The session at the left could not be restored. Reopen it from File → Recent sessions, or close its tab.* (and the island at Left says *“<session>” could not be restored — its files are missing or unreadable. Reopen it from File → Recent sessions, or close this tab.* — never *No session is open* one pane over); a non-Coding host → *Nothing open here.* + *Open a view from the View menu.* `WorkbenchShell.ContentFor` composes it for the projection's placeholder from THIS host's zones and the live registry; `WorkbenchAdapter.Render` rebuilds the placeholder every render (it held no state, and a reused element would keep the sentence of the render before).
- **Oracle:** `tests/AiDe.App.Tests/Workbench/CenterEmptyCopyTests.WhileASessionIsOpenAtLeft_TheCenterSays_TheSessionIsDockedAtTheLeft_NeverNoSessionOpen` (four states in one composed run: none → open at Left → the Left collapsed → closed again; the action present/absent with its name, accelerator and chip; the heading's automation peer exists, is a Control element, its name is the sentence and its help the line; no focusable panel; never *not available in this build*), `WhileARestoredSessionAtLeftCouldNotBeRevived_TheCenterSays_NothingOpenHere` (the Center's and the island's sentences), `TheCopy_IsAFunctionOfTheZonesAndTheLiveRegistry` (the table, seven rows).
- **Red observed before green:** yes — the placeholder rendered the factory's fallback *“Welcome” is not available in this build.* in every state (*Item not found in collection* for *No session open.*). The pure theory was green on its first run (the function was written whole before the wiring); the composed facts carried the red.
- **Confidence:** Verified.

### Claim 5 (F-1) — the reconcile applies a drag while a collapsed zone holds panes or a stack is maximized; the guard's honesty is kept
- **Evidence:** `ZoneBackedLayoutService.TryMapByPosition` rewritten: (1) identity first — `WorkbenchAdapter.BuildPane` writes `((ILayoutPaneSerializable)pane).Id = stack.Id` and `MapNode` reads it back, so a pane that survived the gesture names its zone; (2) the placeholder identifies the Center and never enters the model; (3) share-of-owned majority (greedy over (zone, column) by descending share) for a column no pane names; (4) elimination for the Center, refusal when ambiguous, "every column claimed by a side and no Center column → the Center is empty"; (5) a collapsed zone still holding panes keeps the model's content; a column dropped on its side joins it and expands it; (6) the surface-set guard unchanged. `ReadLayoutFromView` treats the placeholder as always known and never counts it. `RefusedReconcileAnnouncement` is one sentence — the collapsed-panel cause it named is gone.
- **Oracles:** `tests/AiDe.Core.Tests/Workbench/ReconcileTests` — `ADragWhileAZoneIsCollapsedHoldingPanes_IsApplied` (theory: Bottom/Left/Right collapsed-holding; the drop lands, the rail keeps its panes and stays collapsed), `ADragWhileAStackIsMaximized_IsAppliedOrRefusedWithTheTrueCause` (Ruling 47's shape, the operator's finding 4 exactly: the maximized session dropped to the left of the placeholder joins the collapsed Left, which expands; the Bottom's terminal untouched), `TheSessionDraggedIntoTheEmptyCenterAndBack_IsAppliedBothWays_AndThePlaceholderNeverEntersTheModel` (O-2's gesture), `ACenterTabDraggedBesideTheSession_LeavesTheSessionInTheLeft`, `ADropBesideACollapsedHoldingZone_JoinsItAndExpandsIt`, `AViewThatLosesARenderedSurface_IsStillRefused`. Through the real docking host: `WorkbenchDragCompletedHookTests.ADragWhileACollapsedZoneHoldsPanes_IsAppliedThroughTheDockingHost_AndNothingIsRefused` and `InCoding_TheSessionDraggedIntoTheCenterAndBack_FollowsTheDrops_WithTheBottomCollapsed` (zero `placement: refused` records of any cause). `ZoneBackedLayoutServiceTests.ReconcileFromView_WithACollapsedZoneStillHoldingPanes_AppliesTheDrag_AndKeepsTheRailsPanes` re-points INV-0006 F3's test, which pinned the refusal.
- **Red observed before green — recorded per row against the pre-fix mapping** (`ZoneBackedLayoutService.cs` at `9b5d1062^` under the new `CodingDefault`; `red-run-core-reconcile.txt`): `ADragWhileAZoneIsCollapsedHoldingPanes_IsApplied(Left, Bottom)` and `(Right, Left)` — *the reconcile refused a drag because a collapsed zone still held panes*; `(Bottom, Left)` — **APPLIED but WRONG**, *Expected Left, Actual Center* (a raw-count majority tie: a Left of one surface vs a Center of two after one moved let the Center steal the Left's column); `ADragWhileAStackIsMaximized_…` — *refused … still blind to the collapsed zones' panes*; `TheSessionDraggedIntoTheEmptyCenterAndBack_…` — *the drag into the empty Center was refused* (the placeholder is view-only and anchored nothing); `ACenterTabDraggedBesideTheSession_…` — the Left came back **empty** (the session moved); `ADropBesideACollapsedHoldingZone_…` — refused; `ADropOnTheBottomEdge_WhileTheBottomIsCollapsedHolding_…` — the Left came back empty (the vertical root read as one column); `AVerticalRootThatIsNotColumnsOverBottom_IsRefused` — applied (a guess). Only the guard's own test was green, as it should be. Through the docking host, the drag back was *view-unreadable* (the placeholder left in the view was unknown to the projection after the first reconcile) — a silent revert, fixed in `ReadLayoutFromView`. **The WPF lens's finding, red-first:** a drop on the bottom edge while the Bottom is collapsed-holding made a vertical root the mapping read as one column (every Center tab to the Left); the root's second child is now the Bottom's drop (joins the rail and expands it) and any other vertical shape is refused.
- **A premise corrected (recorded, as DC-135 records Ruling 47's):** Ruling 88 says *"(c) is the shape the reconcile refuses … The default would refuse every drag"*. Measured: the Bottom was never pre-seeded, so a collapsed-holding **Bottom** was already kept; the Left and Right were the blind zones — Ruling 47's maximize collapsed both, which is why the operator hit the refusal. The default alone would not have refused; it would have mis-anchored on the tie above.
- **The refusal that remains, through the docking host:** `WorkbenchDragCompletedHookTests.ADragThatCannotBePlacedWithoutGuessing_IsRefusedAndAnnounced_AndThePanesReturn` — a frame with two unnamed columns and no Center column, presented as one docking-model update: `position-mapping-refused` recorded, the model untouched, the strip's sentence *That pane move could not be applied, so the panes will return to where they were.* (the Test Architect's finding 6: the announcement had lost its host-level pin when F-1 re-pointed the old test).
- **Confidence:** Verified (the Core mapping and the docking-host gestures the harness can drive). **Residual risk:** AvalonDock gestures the harness does not drive (float and re-dock) fall back to majority — the mapping refuses rather than guesses when ambiguous; pane identity across those gestures is Inferred (AvalonDock moves documents, not panes — the package ships no source); the maximize memo is not cleared by a drag (a finding below).

### Claim 6 (L5) — Coding's Left extent holds the 96ch measure at the startup size, measured on the composed tree
- **Evidence:** `tests/AiDe.App.Tests/Workbench/ComposedCoding.cs` — the real `WorkbenchShell`'s Coding root inside a frame cut like `MainWindow.xaml` (menu · title strip · 56 px rail + body · status strip), shown at an outer size. **1440 × 900:** body 1369 · Left pane 771 · document 751 · the words' column 693 · 96ch = 672.6 px (Segoe UI 13 px; `ThreadFeed.MeasureWidth`) · the words 662.7 (wrapped at the measure, within one word of it). The 1.3 extent is promoted from Inferred (the mockup's `1.3fr 1fr`) to measured. CV-5.4's headless fixture assumed a 673 × 748 document (D3 §2c's mockup numbers): the real document is 751 × 708 — wider and shorter (the frame's chrome and the pane's tab strip).
- **Oracle:** `CodingsLeftExtentTests.CodingsLeftExtent_HoldsThe96chMeasureAtStartupSize` — the session at Left (precondition), a Center column beside it, the Left pane > half the body, the column ≥ 96ch + 12, the words in [96ch − 80, 96ch + 0.5]. `…AtTheOperatorsSize` the same at the largest window the display gives.
- **The 2560 × 1600 number:** the measuring display **is** a 2560 × 1600 panel — at 150 % scaling (`VisualTreeHelper.GetDpi` 1.50; the virtual screen 1707 × 1067 DIPs), so the largest window it gives is **1728 × 1117 DIPs**, and a "2560 × 1600" WPF window (2560 DIPs, a 100 %-scaled panel) is not reachable here. At that largest window (an earlier, unconditional run; the row printed *clamped by the display, NOT the requested size*): body 1653 · Left pane 931 · column 853. **If the operator's panel runs at 150 % these are their numbers; at 100 % they are an attended row.** CV-5.4's `assume:` (1197 px at 2560, scaled from 673) is **not confirmed and is inconsistent with the extent**: the Left is 0.563 of the body (771 ⁄ 1369; 931 ⁄ 1653), so a 2560-DIP window would give a Left of about 1400 px — Inferred by proportion.
- **Red observed before green:** yes — *Expected Left, Actual Center* on the precondition (the session was in the Center at HEAD), and — the Test Architect's condition — **the extent's own red by mutation**: with `CodingLeftExtent` set to the tool default 0.22 the row read *body 1369 · Left pane 300 · document 280 · words column 205 · 96ch = 672.6* (*the Left pane is 300 of a 1369 body — not the document zone's 1.3 cut*), and L6 read *0 of 2* turns half visible in that column (`red-run-l5-mutation.txt`); restored, 771 / 693 / 1 of 2.
- **The startup size (the Test Architect's finding, ruled here):** `MainWindow.xaml` opened the window at **1180 × 720** while the design's contract is measured *"at the shell's startup size, 1440 × 900"* (DESIGN.md, D2/D3, ratified) — at 1180 the Left would be ≈ 625 px and the column ≈ 547, short of the measure. The markup now says what the design says (`Width="1440" Height="900"`; `MinWidth`/`MinHeight` unchanged); `CodingsLeftExtent_HoldsThe96chMeasureAtStartupSize` reads the XAML's size, asserts it is the design's, and measures at it — a window that opens smaller than the contract fails the row.
- **The operator's size is a row only where the display can give it:** `CodingsLeftExtent_HoldsThe96chMeasureAtEveryViewportTheDisplayGives` and L6's rows come from `MemberData` that adds 2560 × 1600 only when `SystemParameters.VirtualScreen` is at least that — absent from this run (1728 × 1117), not green under the operator's name. The words band is the fixture's longest word measured in the words' own type (58.8 px), not a literal.
- **Confidence:** Verified at 1440 × 900 (and, in the earlier unconditional run, at the display's 1728 × 1117); Inferred at 2560 × 1600.

### Claim 7 (L6) — with the session docked Left, the Bottom collapsed and the editor at 280, the thread holds ≥ 1 turn at 1440 × 900
- **Evidence:** `CodingsLeftExtentTests.AtStartupSizeDockedLeftBottomCollapsed_TheThreadHoldsOneTurn_WithTheEditorAt280` — the editor at 280.0 in every row. **1440 × 900** (document 751 × 708, thread viewport 237): the review's fixture (b4 carries 77 tool calls, 2018 px) → **1 of 2** realized at least half visible (b5 345 px, 237 shown); five plain-prose turns (205 px each) → **1 of 3** (b5 fully shown). **1728 × 1117** (the display's cap for the 2560 × 1600 request; document 911 × 892, viewport 421): the fixture → 1 of 3 (b5 345 shown); prose → **2 of 4** (b4 and b5 fully shown). Threshold applied: ≥ 1 at both.
- **Oracle:** the theory's rows (`DensityRows`: the startup size with both fixtures; 2560 × 1600 only on a display that gives it); each prints the viewport, the editor, the count and every realized turn's height and shown pixels.
- **Red observed before green:** yes — on the precondition (the session was in the Center), and by the extent mutation (0.22 → *0 of 2* at 1440 × 900 with both fixtures — a 280 px column makes every turn taller than the viewport). **Residual (the Test Architect's finding 8):** the ≥ 1 threshold passes for any viewport at least half the last turn's height (≈ 103 px prose); the row prints the viewport (237) so a starved thread is visible in the record, not asserted — Ruling 88 c3 names no floor.
- **Confidence:** Verified at the two measured sizes. **For the Owner (CV-5.4's finding stands):** the count is dominated by a single tall turn; a plain-prose thread shows 2 at the larger size. `DESIGN.md`'s row now states the measured numbers and marks 2560 × 1600 as Inferred pending the operator's display.

### Claim 8 (C5) — the Console split is a `console` document in the Center, one per session, closing with its session
- **Evidence:** the `console` row (`"console"`, "Console", *One session's Console: every wire frame, one row per message.*, `Perspectives: [Coding]`, `Instances.Many`, `Verb("session.console")`, `Zone: Center`); `WorkbenchCommands` `session.console` ("Session console", `Ctrl+K, Ctrl+L`, `_View`, `Admits("session-document")`); the controller's case → `SessionConsoleRequested(FocusedSurfaceId)`; the shell resolves the session from the focused surface (a session document or its console), `OpenSessionConsole` adds `console:<sessionId>` captioned *Console — <name>* into the Center's stack (a sibling tab when the session is there), `OpeningDocument` gated on applied, renders, `FocusConsole` activates it, lands the caret on the ordinal when given, focuses the split, checks the toggle and announces *Console open, <status>.*; a second toggle → `ShowOpenConsole` (expand a collapsed holder, activate, focus; *Console shown, <status>.*); `ConsoleFor` gives the factory a `ConsoleDocumentHost` over the document's own `Split` (removed from the document's grid; follows `ReadModel.Changed`, marshalled); `OnSurfaceClosed` (the adapter's funnel): a console leaving is disposed, its toggle reflected unchecked, focus returned to the toggle and *Console closed.* announced; a session leaving closes its console. The seam's named touches in `SessionDocumentSurface`: `ConsoleRequested` (the verb) and `ConsoleDismissed` (the close), dispatched from the toggle's **Checked/Unchecked** — the events an AT's Toggle pattern raises with no Click — so the toggle's state IS the document's open state: checking opens or focuses, unchecking closes; `ReflectConsole(open)` lets the shell mirror a state it changed itself without dispatching; `OpenSplit` routes to the verb, so a turn's *Open the log* lands in the document too. The toggle's accessible name when hosted: *Console — open as a document in the Center*; help *Opens the Console as a document in the Center; press again, or close its tab, to close it.* (no chord in operator copy, US-C10 b3 — the View menu's row carries the gesture) (`ConsoleDocumentHost.ToggleName/ToggleHelp`). **A recorded deviation from Ruling 89's letter** (*"a second toggle focuses it"*): a second **press of the toggle closes** the document — the UX & Accessibility lens's Blocker (a ToggleButton that never releases lies to WCAG 4.1.2's Toggle pattern); a second run of the **verb** (`session.console`, the View menu, the palette) focuses the one open console, and a turn's *Open the log* focuses it at the turn. The count stays ≤ 1 either way; the conductor may reverse it in one line (the `Unchecked` handler).
- **Oracle:** `tests/AiDe.App.Tests/Sessions/ConsoleSplitPlacementTests.WithTheSessionAtLeft_TheSplitOpensInTheCenterZone_AsAConsoleDocument` — the toggle pressed through the **UIA Toggle pattern** (`IToggleProvider.Toggle`, the path a Click-driven toggle ignores) in the real shell: one `console` surface, id `console:<id>`, caption *Console — payments extraction*, in the Center and active, never in the Left stack, the rendered content IS `document.Split` (`Assert.Same`), `IsSplitOpen` false, the toggle checked and named for its new home, *Console open, <status>.* announced, the Center's empty copy gone; a second run of the verb leaves the count at 1, focuses, says *Console shown, <status>.*; unchecking the toggle closes it — *Console closed.*, focus on the toggle, the empty copy back; a reopen hosts the same split; closing the session removes it and unchecks the toggle. `CollapsingTheLeftHoldingTheSession_LeavesItsConsoleOpen` (a hide is not a close); `WithTheSessionInTheCenter_TheConsoleIsASiblingTab`; `TheVerb_OpensTheFocusedSessionsConsole_AndRefusesWithNoSessionFocused`; `TheConsoleKindsRow_IsCodingsManyInstancesVerbRow_HomedInTheCenter`.
- **Red observed before green:** yes — no row (*The collection did not contain any matching items*), no verb, and the toggle opened the split inside the document's grid (*Expected Center, Actual null*).
- **Confidence:** Verified. **Residual risk:** a saved arrangement carrying a `console` surface restores the factory's *No session is open* text until the session is revived; the console is not re-bound on revive (a next step: `ReviveRestoredSessionDocuments` could `Invalidate` its console as it does the document).

### Claim 9 (the landing) — Coding lands on the session at Left, else on the Center's empty copy, each with a focus target
- **Evidence:** `Perspective.Landing` Coding = `ZoneId.Left`; `PerspectiveShell.LandingSurfaceFor` — the landing zone's active tab; else the Center's active tab, or the placeholder's id when the Center is empty; else the view's active. `PerspectiveShell.FocusLanding(content)` — a session document lands in its **editor** (`FocusRegion(Composer)`: the page when ready, else the composer's first line), any other surface on its first focusable; `MainWindow.EntryFocusFor` calls it (`MoveFocus(First)` on a session document landed on the header's first button — the UX lens's finding). `CenterEmptyState.Build` — the New session button (first in tab order) or the focusable heading.
- **Oracle:** `PerspectiveShellTests.TheLanding_IsTheRowsZonesActiveTab_LeftForCoordination_CenterForArchitecture_ElseTheViewsActive` extended (Coding: the placeholder, then `session:s1` once opened at Left, then the placeholder again with the Left collapsed; Coordination's fallback assertion unchanged in value — the ledger); `SessionDocumentPlacementTests.TheCodingLanding_IsTheSessionAtLeft_ElseTheCentersEmptyCopy_EachWithAFocusTarget` through the real shell — `Keyboard.FocusedElement` inside the composer after `FocusLanding` on the session, and the copy's heading after `FocusLanding` on the fallback.
- **Red observed before green:** yes — *Strings differ* (the view's active vs the placeholder) and *the Center's empty copy has no focus target*; the editor landing was written after the review and seen red on `MoveFocus(First)` by the lens's reading (the header's first button), then green.
- **Confidence:** Verified for the surface the window focuses and where in it; O-7's Coding half stays an attended row for the spoken text.

### Claim 10 (Ruling 88 condition 2, decided and measured) — the collapsed Bottom's terminal is one gesture away and is not started until the rail expands
- **Decision:** a collapsed zone is absent from the projection, so its pane content — and the shell process a terminal pane starts — is built when the rail is expanded, not at startup. The alternative (a started shell behind a rail) costs a process for a pane the operator's five screenshots never showed open, and the adapter would end it on the next collapse anyway (a finding below).
- **Oracle:** `tests/AiDe.App.Tests/Workbench/CollapsedBottomTests.TheDefaultBottomsTerminal_IsNotBuiltUntilTheZoneExpands` — no content, no `terminal.start` record, the Bottom's rail visible; after the expand: content, exactly one `terminal.start`.
- **Red observed:** no — green on its first run against the committed re-cut: the docking layer already behaved this way for a collapsed zone (a **characterization** pin, labelled as such in the test; a premise confirmed, not a control seen red). The re-pointed suites that assumed a live default terminal (`TerminalSurfaceStopLineTests`, `OpeningATerminalWithNoneOpenTests`, `WorkbenchShellTests`) now expand the Bottom first or read the model; `AppWindowCloseLeavesNoTerminalHostTests` seeds the workspace's Coding slot with the Bottom expanded so the real App starts a host to measure (`SeedATerminalAcrossTheBottom`; the seed is asserted non-redundant).
- **Collapse is a hide, not a close (the WPF lens's two Majors, red-first).** The adapter kept only the projection's surfaces, so collapsing a zone disposed its terminal's process (DC-029 by a new route, no Busy confirmation) and raised this slice's `SurfaceClosed` for the session document — a hide gesture closed the session's console; and expanding a zone holding a retained session document would hand the factory an element still parented to the old island. `WorkbenchAdapter.Render` now keeps what the **model** holds (`HeldByTheModel`): a collapsed-holding zone's content is **parked** (`_parked`), reachable through `ContentFor`, given back on expand, disposed and reported closed only when its surface leaves the model; `SurfaceClosed` fires only for the model's losses; the shell's `AllHostSurfaces` reads the model. **Oracles (red → green, the red messages recorded):** `CollapsedBottomTests.CollapsingTheBottomAfterUse_KeepsTheTerminal_AndExpandingGivesTheSameOneBack` (*Assert.Same … Actual: null* — disposed on collapse; one `terminal.start`, zero `terminal.stop`), `SessionDocumentPlacementTests.CollapsingTheLeftHoldingTheSession_ThenExpanding_GivesTheSameDocumentBack_AndClosesNothing` (*Collection was not empty: ["session-document:…"]* — `SurfaceClosed` on a hide), `ConsoleSplitPlacementTests.CollapsingTheLeftHoldingTheSession_LeavesItsConsoleOpen` (*Expected Center, Actual null* — the console closed by the hide).
- **Confidence:** Verified.

### Claim 11 — the DESIGN.md seam rows (DC-196 reconciled)
- **Evidence:** the drop sentence row now reads the P6 oracle's shipped form — *They live in Coordination (Ctrl+4); open them from its View menu.* / *It lives in …; open it …* — with the reason (*opens with them* asserted Coordination's default, Inferred and false for Daydreams; *live in* is true by the allow-list); the Coding re-cut row carries Ruling 88's collapsed Bottom, the retirement, and the measured extent; the "Turns at least half visible" row is the per-viewport measured threshold with the numbers above.
- **Oracle:** `verify-cited-controls.py` OK (every cited control resolves); the strings the tests assert are the strings the row states.
- **Confidence:** Verified.

## Stage 4 — the reviews (read-only, three, two loops — the cap) and what changed after them

| Lens | Loop 1 | What changed | Loop 2 |
|---|---|---|---|
| **UX & Accessibility** (hard veto on the landing/focus, the copies, the announcements, the console's caption and close) | **VETO HELD** — (1) Blocker: the header toggle's name said *beside the thread* and a press never released; UIA Toggle flipped `IsChecked` with nothing happening. (2) *its recovery is on the left* promised an affordance the island did not offer. (3) the copy's focus target was a `Grid` (no automation peer — silent to an AT). (4) Ctrl+1 landed on the header's first button, not the editor. (5) the console's close dropped focus silently. (6) the mockup's stale *Maximize* note. (7) the New session button named the gesture twice, hard-coded. (8) the non-Coding copy not in DESIGN.md. (9) the collapsed-Left copy. (10) nits. | The toggle is driven from `Checked`/`Unchecked` with `ReflectConsole` (state = open; a second press closes; the verb focuses — the recorded deviation from Ruling 89's letter, `docs/notes/console-toggle-closes-on-second-press.md`); named *Console — open as a document in the Center*; the restore copy names the way out on both the Center and the island; the heading is a `ThreadText` (peer asserted); `PerspectiveShell.FocusLanding` lands in the editor; the close focuses the toggle — or, when the rail hides it, the host's landing one dispatcher turn later — and says *Console closed.*; *New session* + the catalog's accelerator + a visible chip; the collapsed-rail sentence; DESIGN.md's Copy row carries every string. | **VETO CLEARED** with conditions, all taken: the collapsed-Left close's focus fallback (a red-first row in `ConsoleSplitPlacementTests`), the ruling amendment as a filing note under Ruling 89, the help text state-neutral, the island's peer asserted; the chip's pair measured by the census walk (`aide-contrast-census.md` at `6cb916e4`+: *No session open.* 13.57:1, *New session* 13.57:1, the *Ctrl+N* chip 6.48:1 as `TextMutedBrush` on the raised island — its own sunken ground is darker, so higher still; the console toggle named *Console — open as a document in the Center* 13.57:1; the View row *Session console* 14.30:1; 177 pairings, 0 below floor). The docked-at-left copy uses the same two token pairs and was not itself walked (the census walks host A before it opens the session) — a token identity, not a measurement of that state. The deviation from Ruling 89 is **acceptable from the UX side, explicitly**. |
| **Test Architect** (hard veto on F-1/C5 red-first, L5/L6 measured) | **VETO HELD** — (1) L5/L6 red only on a precondition, never on the geometry; (2) the window opened at 1180 × 720 while the contract says 1440 × 900; (3) no run record for the Core reds; (4) the Bottom-row remark contradicted the run; (5) `CollapsedBottomTests` green-first; (6) the refused-drag announcement lost its host-level pin; (7) the words band a literal; (8) L6 tolerates a starved viewport; (9) the 2560 rows passed under the operator's name; (10) replica drift; (11) re-points fine. | The extent mutation red recorded (`records/sh-4-2/red-app-l5-l6-extent-mutation.txt`); `MainWindow.xaml` opens at 1440 × 900 and the startup fact reads the XAML; the per-row Core red recorded against the pre-fix mapping; the remark corrected; the characterization labelled; the ambiguous-frame host row pins the sentence and the panes' return; the band is the fixture's longest word measured in its type; the 2560 rows are `MemberData` present only where the display gives them, and the startup row prints the display's DIPs, scale and reach. | **VETO CLEARED** with conditions, all taken: the parking reds recorded to a file (`red-app-parking-adapter-at-6cb916e4.txt`), the current-code green attached (`green-app-at-close.txt`), the ambiguous-frame row asserts the panes' return, the Core record carries its git context. Residuals accepted: L6's ≥ 1 tolerates a starved viewport (the row prints it; Ruling 88 c3 names no floor); float/re-dock gestures not driven. |
| **WPF lens** (the collapsed Bottom; the docking reconcile) | **NO ESCALATION**; two Majors: (1) collapse rendered as close — a hidden zone's terminal disposed, its process killed with no Busy confirmation, and `SurfaceClosed` raised for a hidden session so a hide closed its console; (2) expanding a zone holding a retained session document would throw (*already the logical child of another element*). Minors: `TerminalSurface` is not an HwndHost (the `Windowed` row records a wrong mechanism); a vertical root with the Bottom absent read as one column; pane identity across float/re-dock Inferred; `CurrentRegion` reports Split for the hosted console; `ZoneRails` brushes resolved once. | Content of collapsed-holding zones is **parked** and given back on expand; `SurfaceClosed` only for the model's losses (three red-first oracles); a vertical root's second child is the Bottom's drop, other vertical shapes refused (two red-first Core rows). The minors are findings below. | not re-run (no escalation held; the two Majors' fixes are red-first with the Test Architect's mutation reading). |

**Simplifier's delete-list** (run by the author over the diff, the three reviews having spent the fan-out): `delete:` the collapsed-panel branch of `RefusedReconcileAnnouncement` (a cause F-1 removed, −8); `shrink:` `ExpandZone`'s zone switch into `ZonesToTree.StackIdOf` (−10; one definition of the projected ids); `delete:` `Rendered()` in `TryMapByPosition` once the vertical root was read directly (−1); `yagni:` no second `ConsoleSurface` — the document's own instance is hosted; `stdlib:`/`native:` none. `net: −19 lines possible`, taken.

## Re-pointed tests (the amended rulings, never a weakening)

| Test | Was | Now |
|---|---|---|
| `ZoneBackedLayoutServiceTests.ReconcileFromView_WithACollapsedZoneStillHoldingPanes_RefusesEveryDrag` | pinned INV-0006 F3's refusal | `…AppliesTheDrag_AndKeepsTheRailsPanes` — applied; the rail keeps its panes |
| `WorkbenchDragCompletedHookTests.ADragThatCannotBeApplied_IsAnnouncedInsteadOfSilentlyReverting` | the collapsed-panel refusal announced | `ADragWhileACollapsedZoneHoldsPanes_IsAppliedThroughTheDockingHost_AndNothingIsRefused`; plus `InCoding_TheSessionDraggedIntoTheCenterAndBack_…` (SH-4.1's handed-off Coding drag row) |
| `Sessions/ANewSessionTakesTheWholeTreeTests` (Ruling 47's condition) | create maximizes; reopen does not; the call is on the create path | deleted; `SessionDocumentPlacementTests` (Ruling 47 c1 re-pointed to Ruling 83) |
| `PerspectiveShellTests.TheLanding_…` | Coding's `Landing` null; falls back to the view's active | Coding = Left; the fallback is the Center's active tab (the ledger row's value unchanged) |
| `PerspectiveMenuTests.EveryEntryVerbRowNamesAGlobalCatalogCommand_…` / `TheAllowListsEqualTheSpecsTable` / `TheRenderedMenuEqualsTheSpecsLiteralTable_…`; `KindAllowListsTests` | two verb rows; §A7 without `console`; Coding's View without *Session console* | the `console` row (a View-scoped verb, not an entry verb); Coding's column gains `console` |
| `EveryOpeningCommandPassesThroughTheSeamTests` | three heads | `OpenSessionConsole` is a head (the focus branch split into `ShowOpenConsole` so the add carries its seam) |
| `TerminalSurfaceStopLineTests.TheShellsDispose_…`, `OpeningATerminalWithNoneOpenTests` (×2), `WorkbenchShellTests.Shell_Composes…` | a live default terminal in the projection | the Bottom expanded first / the model read; the composition test asserts the terminal in the model and absent from the projection |
| `AppWindowCloseLeavesNoTerminalHostTests` | the default's terminal starts at attach | the workspace's Coding slot seeded with the Bottom expanded |
| `ComposerHostIntegrationTests` (×4) | `--shell --maximize` | `--shell` (one choreography) |

## Findings (not this slice's to fix)

1. **The maximize memo is never consumed.** `ZoneLayoutService.Restore` has no caller; `workbench.maximizePane` toggles on the *projected* `StackState`, which the zone projection never sets, so its second press sends `Maximized` again and is refused (*Something is already maximized; restore it first.*). A drag while maximized (F-1) leaves the memo set. DESIGN.md's *"[Inferred: `workbench.maximizePane` toggles]"* is false on the zone service. For the conductor (Shell lane).
2. **`SurfaceContentFactory`'s `terminal` row says `Windowed: true`** and `SurfaceChrome`'s note reasons about an HwndHost, but `TerminalSurface` is a `ContentControl` over a `TerminalView : FrameworkElement` — no HWND in the terminal path (the WPF lens). A wrong mechanism recorded; the terminal could take the island. For the conductor (the census's contrast walk would then cover the terminal's chrome).
3. **Reopening a closed session document would fault.** A closed session document stays in `_sessionDocuments` parented to a dead island; a reopen hands the factory the same element and `WrapAsIsland` would throw *already the logical child of another element* — pre-existing, untouched here (the console host avoids the shape by releasing its content on close).
4. **The mockup's Coding note contradicts DESIGN.md:** `perspective-shell.html` line 775 says *"with Maximize (Ctrl+K, Z) as the first action"*; DESIGN.md's row says **no first action**. The copy landed per DESIGN.md (the ruling's rationale: a Maximize would hide the zone that offers it). For D3.
5. **The session document's own region cycle** (`CurrentRegion`/`CycleRegion`) reports `Split` when focus is in the Center console while `IsSplitOpen` is false, so F6 skips the Console and `FocusRegion(Split)` refuses — inconsistent, not hazardous (the WPF lens); the Console is reachable by its tab and `Ctrl+K, Ctrl+L`. A Conversation-lane file — for CV.
6. **A `console` surface in a saved arrangement** restores the factory's empty text until its session is revived, and is not re-bound on revive (Claim 8's residual).

## Attended rows (RUN-PENDING; the conductor runs what a file or an exit code can evidence)

| Row | Steps | Evidence |
|---|---|---|
| **O-1** Ctrl+N docks Left | 1. Launch the Release build against a workspace. 2. Ctrl+N, name a session, create. 3. The document is in the LEFT pane at about 56 % of the body; the Center reads *The session is docked at the left.* / *Code viewers, prompt drafts and search open here, from the View menu.*; the status strip's sentence contains no *maximized*. | the workbench log's `layout.mutation` line: `operation: open-session-document, placement: zone-rule`; a screenshot |
| **O-2** drag Center and back | 1. With the session at Left, drag its tab onto the Center pane. 2. Drag it back to the left edge of the Center pane (a new column). 3. Neither drag reverts; no *could not be applied* sentence. | `layout.mutation` lines `operation: drag, placement: reconciled` with `moved: ["session:…"]`, no `refused` |
| **O-7** (Coding half) Ctrl+1 lands on the session | 1. With the session at Left, Ctrl+3 then Ctrl+1. 2. Focus is inside the session document (NVDA reads its header or editor), never a tab header. | `Keyboard.FocusedElement`'s surface (the census log's *active after switch*) |
| **The pre-C layout report** | 1. Launch against the operator's 2026-09-13 workspace (the pre-C Coding slot). 2. The strip reads *4 panes from your saved layout aren't available in Coding — Ledger, Leaderboard, Sessions (terminal sessions), Board (message board). They live in Coordination (Ctrl+4); open them from its View menu.* | the sentence; `layout.restore` `dropped_count 4` |
| **L5/L6 at 2560 × 1600** | On the operator's display: run `CodingsLeftExtentTests` (the 2560 rows print *given 2560×1600* with no clamp note) and read the Left pane, the column and the half-visible counts. | the test output |
| **The terminal one gesture away** | 1. Fresh workspace: the Bottom rail shows *Terminal — pwsh* collapsed; no `powershell.exe` under the App. 2. Click the rail: the terminal starts. | the process table; `terminal.start` |

## Defect classes registered (CI1) — placeholders, never the register itself (the conductor allocates; the next free number follows DC-201)

- **DC-nnn (SH-4-2 a)** — *a content-majority anchor by raw count lets a large zone that lost one tab steal a small zone's column on a tie, moving the bystander* (DC-063's class by another mechanism): a Left of one surface and a Center of two after one moved both counted one in the Left's column; the Center won the tie by rule and the drag moved the SESSION to the Center. **Control:** anchors by pane identity first (`ILayoutPaneSerializable.Id`), then share-of-owned; `ReconcileTests.ACenterTabDraggedBesideTheSession_LeavesTheSessionInTheLeft`; the theory's Bottom row.
- **DC-nnn (SH-4-2 b)** — *a view-only synthetic surface the projection drops after a model change becomes unknown to the view reader, and every later gesture is "unreadable" until a render*: the Center's placeholder stayed in the view after a drag into the empty Center reconciled the model; the drag back was `view-unreadable` — a silent revert with no sentence. **Control:** `ReadLayoutFromView` treats the placeholder as always known and never counts it; `WorkbenchDragCompletedHookTests.InCoding_TheSessionDraggedIntoTheCenterAndBack_…` (zero refusals of any cause).
- **DC-nnn (SH-4-2 c)** — *a synthetic view surface with no kind row falls through to the factory's "not available in this build"* — a build-defect sentence for the product's ordinary empty state, in every state. **Control:** the shell composes the Center's copy from the model (`CenterEmptyState`); `CenterEmptyCopyTests` assert the sentence is never rendered.
- **DC-nnn (SH-4-2 d)** — *a refusal sentence names a mechanism as the cause after a fix removed that mechanism* (`RefusedReconcileAnnouncement`'s "a collapsed panel still holds panes" after F-1). **Control:** the sentence names only what the code can still tell; `ADragWhileACollapsedZoneHoldsPanes_IsAppliedThroughTheDockingHost_…` asserts the old sentence is absent. Sweep: the same sentence is a fixture string in `TheStatusStripClearsOnSupersessionOrDwellTests` (X-3's; a fixture, not a claim — left).
- **A premise corrected, not a class:** Ruling 88's *"the default would refuse every drag"* — the collapsed Bottom was already kept by the mapping; the Left/Right were the blind zones (Claim 5).

## Instrumentation (IO1–IO12)

| Operator's question | Emitting source | Value seen |
|---|---|---|
| Where did the session open, and by what rule? | `layout.mutation` `operation: open-session-document`, `placement: zone-rule` | `zone-rule` (L2's run) |
| Did the drag apply, and what moved? | `layout.mutation` `operation: drag`, `placement: reconciled|refused`, `moved`, `refusal` | O-2's two lines `reconciled`, `moved: ["session:s1"]`; `refused` count 0 (the docking-host tests) |
| Did the console open, where? | `layout.mutation` `operation: open-console`, `placement: zone-rule` | C5's run |
| Did the terminal start at launch? | `terminal.start` (none until the rail expands) | 0 → 1 (`CollapsedBottomTests`) |
| What does the Center say? | the rendered copy; the accessible name | the census walk (host A with a session at Left) |
| **Gap:** which surface the switch landed on | none in the product (SH-4.1's gap, still open — `WorkbenchDiagnostics.cs` is X-3's live file) | the census log's *active after switch* |

## Records (committed beside this pack: `docs/proof/records/sh-4-2/`)

- `red-app-at-head-36b7210e-plus-9b5d1062.txt` — the App oracles against the product at `9b5d1062` (Core re-cut landed, App untouched): 18 red of 24.
- `red-core-reconcile-prefix-mapping.txt` — `ReconcileTests` against `ZoneBackedLayoutService.cs` at `9b5d1062^`: 9 red of 10, per row.
- `red-app-l5-l6-extent-mutation.txt` — `CodingLeftExtent` mutated to 0.22: L5 and L6 red on the geometry.
- `red-app-parking-adapter-at-6cb916e4.txt` — the three parking oracles against the adapter at `6cb916e4`: 3 red of 3.
- `green-core-at-close.txt`, `green-app-at-close.txt` — the suites at close, with L5/L6's measured lines and the display's reach line.

## Gates at close

*(filled at close.)*
